# Wild West Multiplayer — מסמך המשכיות

מסמך זה נכתב כדי שאפשר יהיה להמשיך את הפרויקט בצ'אט חדש בלי לאבד הקשר.
**העלה אותו בתחילת השיחה הבאה.**

---

## 1. מה זה

משחק FPS בדפדפן בסגנון מערב פרוע (Three.js r128, קובץ HTML יחיד), שהופך
בהדרגה למולטיפלייר אמיתי דרך Node + Express + Socket.IO.

| | |
|---|---|
| Repository | `amitshellef-ui/wild-west-multiplayer` (GitHub, העלאה ידנית דרך הדפדפן) |
| אירוח | Render — build `npm install`, start `npm start` |
| כתובת | https://wild-west-multiplayer.onrender.com |
| עבודה מקומית | `C:\Users\tamip\Desktop\CLOUD ME\משחק\` |

Render ב-Free tier נכבה אחרי חוסר פעילות — החיבור הראשון אחרי הפסקה לוקח
30-50 שניות. זו לא תקלה.

---

## 2. איפה אנחנו עומדים

התוכנית המקורית הייתה 16 שלבים. **שלבים 1-12 הושלמו, נבדקו והועלו.**

| שלב | מה | סטטוס |
|---|---|---|
| 1 | חיבור Socket.IO | ✅ |
| 2 | זיהוי שחקנים מחוברים | ✅ |
| 3+4 | סנכרון מיקום וכיוון (אוחדו) | ✅ |
| 5 | גופים לשחקנים אחרים | ✅ |
| 6 | תנועה חלקה (אינטרפולציה + הליכה) | ✅ |
| 7 | סנכרון ירי | ✅ |
| 8+9 | נזק וחיים עם סמכות שרת (אוחדו) | ✅ |
| 10 | מוות והחייאה | ✅ |
| 14-16 | חדרים, קוד, הצטרפות (אוחדו, **הוקדמו**) | ✅ |
| 11א | מפה דטרמיניסטית + ניווט בשרת | ✅ |
| 11ב | בנדיטים משותפים בשרת | ✅ |
| 11ג | ירי בנדיטים בשרת | ✅ |
| 12 | בוס לשרת | ✅ |
| **13** | **גלים לשרת** | ⬜ הבא |

**החדרים הוקדמו לפני הבוטים המשותפים בכוונה** — אחרת היינו בונים סימולציית
בוטים גלובלית ואז מפרקים אותה לכל חדר. עבודה כפולה על החלק הגדול בפרויקט.

---

## 3. הקבצים

| קובץ | תפקיד |
|---|---|
| `wild-west-fps-arsenal.html` | כל המשחק — ~220KB, קובץ יחיד |
| `server.js` | Express + Socket.IO, חדרים, שחקנים, אימות פגיעות, לולאת הסימולציה |
| `bandits.js` | סימולציית הבנדיטים בשרת: ספawn, ניווט, מרדף, ירי, קליעים |
| `boss.js` | **חדש (12)** — הבוס בשרת: 8 טיפוסים, יכולות, טיימר, דינמיט ורעל |
| `navigation.js` | התנגשויות + A* בשרת — **תרגום מדויק של הקוד שבלקוח** |
| `map-data.js` | **קובץ מיוצר** — 134 תיבות התנגשות של המפה |
| `package.json` | express ו-socket.io, גרסאות נעולות |

**חשוב**: `map-data.js` מיוצא מלקוח רץ. אם המפה בלקוח משתנה — צריך לייצא
מחדש ולוודא ש-`FINGERPRINT` תואם למה ש-`mapFingerprint()` מדפיס בלקוח.

**חשוב לא פחות**: טבלת `BOSS_TYPES` קיימת **בשני הצדדים** — ב-`boss.js`
וב-HTML. השרת שולח **אינדקס** לתוך המערך, כך שהסדר שלו הוא חלק מפרוטוקול
הרשת. אין למיין אותו מחדש. יש בדיקה אוטומטית שמשווה את שתי הטבלאות.

---

## 4. הארכיטקטורה — מה רץ איפה

### השרת מחזיק
- מי בכל חדר, ומצב החדר (מוד, בנדיטים, בוס)
- **חיים של שחקנים** ומי מת ומתי חוזר
- **טבלת הנזק של כל הנשקים** — הלקוח לעולם לא אומר כמה נזק
- כל סימולציית הבנדיטים: מיקום, AI, ירי, קליעים
- **כל סימולציית הבוס**: איזה מהשמונה, מתי מגיע, איך נע, היכולת שלו,
  הקליעים שלו, הדינמיט, ענן הרעל, ומה כל אלה עולים בחיים
- אימות: טווח, קצב אש, מספר כדוריות, חדר משותף

### הלקוח מחזיק
- כל הרינדור, הקול, ה-VFX
- תנועת השחקן עצמו
- **כל המשחק לשחקן יחיד** — כשאין שרת, הכל רץ מקומית כמו שהיה

### מודל האמון
הלקוח מדווח **שפגע ואיפה בגוף** — לא כמה. השרת דוחה: נשק לא חוקי, קצב
בלתי אפשרי, מספר כדוריות שהנשק לא יורה, מרחק מחוץ לטווח, פגיעה בעצמך,
ופגיעה בין חדרים. זה תקף גם ל-`bandit-hit` וגם ל-`boss-hit`.

**מה שעדיין לא נאכף**: קו ראייה בפגיעות שחקן-בשחקן ובפגיעות בבוס. רמאי
יכול לטעון פגיעה דרך קיר. זה דורש בדיקת גיאומטריה בשרת לכל ירייה.

**מה שנסגר ב-12**: נזק מהבוס. השרת יורה, זורק ומפוצץ בעצמו.
`health-delta` נשאר **להתאוששות ולבונוס הריגה בלבד** — עם תקרות
(מקסימום 20 לדיווח, 5 דיווחים בשנייה).

---

## 5. החלטות שחשוב להבין

**מפה דטרמיניסטית.** המפה נבנתה אקראית בכל טעינה — כל שחקן ראה סלעים
במקומות אחרים. הפתרון: `Math.random` מוחלף ב-PRNG עם זרע **רק לאורך בניית
המפה** (`MAP_SEED` בלקוח), ומוחזר מיד אחרי. אימות: שני לקוחות מדפיסים
`mapFingerprint()` זהה.

**דיוק מלא ב-map-data.js.** עיגול לשתי ספרות הפך 4 תאי ניווט ש*נוגעים*
בקיר לתאים ש*חופפים* לו, והשרת והלקוח לא הסכימו איפה בנדיט יכול ללכת.
אחרי ייצוא בדיוק מלא: **0 אי-התאמות ב-14,112 תאים.**

**אינטרפולציה של 110ms.** גופים מרוחקים מצוירים מעט בעבר, כדי שתמיד תהיה
חבילה משני הצדדים. `INTERP_DELAY` בלקוח. הבוס מצויר באותה שיטה.

**קליעים נוסעים, לא פגיעה מיידית.** גם בשרת. כדי שאפשר יהיה להתחמק.
הבוס חולק את רשימת הקליעים של הבנדיטים, אבל כל קליע נושא את המהירות והנזק
שלו — קליע של צלף נוסע 72 מ'/ש' ועולה 32, של בנדיט 42 ועולה 10.

**מצב שיתופי הוא ברירת המחדל.** `MODES` ב-`server.js`, `MODE = MODES.coop`.
באש ידידותית כבויה שחקנים **לא נמצאים בנתיב הירי בכלל** — הכדור עובר דרך
בן ברית ופוגע בבנדיט מאחוריו. נאכף גם בשרת, לא רק בלקוח.

**חדר מתאפס כשהוא עובר מריק למאויש.** אחרת הנכנס הבא יורש 12 בנדיטים
ובוס פצוע למחצה. מאופס גם המונה ל-90 שניות.

**בנדיטים לא קמים לתחייה כשבוס בשטח.** בדיוק כמו בדפדפן — אחרת קרב בוס
הוא קרב בוס ועוד תריסר בנדיטים טריים. `room.bossAlive` נקרא מ-`bandits.js`.

**הרווח היחיד מהמקור: הטלפורט של הרוח.** בדפדפן `GHOST-WALKER COLE` קפץ
ל-`randomNavPoint()` אקראי על כל המפה, כלומר ב-80% מהמקרים 40 מטר משם —
מחוץ לטווח הירי שלו, ומחוץ ליכולת של מישהו למצוא אותו. באונליין זה גרוע
יותר: בוס שאי אפשר להרוג עוצר את כל מחזור הבוסים של החדר לנצח. לכן הקפיצה
מוגבלת ל-8 עד 26 מטר מהשחקן (`blinkMin`/`blinkMax` ב-`boss.js`). נמדד:
0 נזק לדקה לפני, ~600 אחרי. אם רוצים את ההתנהגות המקורית בדיוק — להחזיר
את הבדיקה לנקודה אקראית כלשהי.

---

## 6. מלכודות שהתגלו בדרך

- **לשונית ברקע מפסיקה לשדר.** הדפדפן מקפיא `requestAnimationFrame`. כדי
  לבדוק שני שחקנים לבד — **שני חלונות נפרדים, לא שתי לשוניות.**
- **קצב שידור = min(20Hz, fps).** אם ה-fps נמוך, גם השידור.
- **`Box3.intersectsBox` סופר נגיעה כחפיפה.** מכאן בעיית העיגול.
- **קליע מהיר מדלג מעל מטרה.** 2.1 מ' לטיק מול ספירה ברוחב 1.24 מ' = 37%
  מהפגיעות הישירות אבדו. הפתרון: בדיקת מרחק למקטע, לא לנקודה.
- **`/` מחזיר 404 בלי redirect.** יש redirect ב-`server.js`.
- **בדיקות headless חייבות שעון שמתחיל מ-`Date.now()`.** `bandits.js`
  חותם טיימרים מול השעון האמיתי, אז שעון וירטואלי שמתחיל ב-2023 גורם לכך
  שאף בנדיט לא יורה ואף אחד לא מבין למה. `boss.js` מקבל `now` כפרמטר
  בכל פונקציה שצריכה זמן, בדיוק בגלל זה.

---

## 7. איך נבדק כל שלב

לא "נראה בסדר" — מדידה. הדפוס שחזר על עצמו:

1. שרת מקומי על פורט 3000 עם עותק של כל הקבצים
2. שני לקוחות בדפדפן, השוואת מצב ביניהם
3. הרצות headless ב-Node לסימולציות ארוכות ולמדידת ביצועים
4. ניסיונות תקיפה מפורשים על כל נתיב אימות בשרת
5. בדיקת רגרסיה שמצב אופליין לא נשבר — **בכל שלב**

**מה שנמדד בשלב 12:** כל 8 הבוסים 60 שניות כל אחד ×5 הרצות (כולם גורמים
נזק, אף אחד לא הולך דרך קיר); 10 דקות × 4 חדרים מלאים בלי דליפה (קליעים,
מפגעים וענני רעל מתנקזים, בנדיטים נעצרים על 12); עלות טיק — הבוס מוסיף
1.6ms בממוצע על 4 חדרים, והקפיצות הן של ה-A* ולא שלו; 19 בדיקות על סוקט
אמיתי (נזק מדויק לפי הטבלה, cooldown, מספר כדוריות, טווח, חדר זר, זבל
בפאקטות, הריגה, זקיפה לשחקן, הבוס הבא ברשימה); 10 בדיקות הצטרפות באמצע
קרב; והשוואה אוטומטית בין שתי טבלאות ה-`BOSS_TYPES`.
עלות רשת: **~690 בתים לשנייה לשחקן** לכל הבוס.

---

## 8. מספרים למי שמכוון

ב-`bandits.js`, אובייקט `BANDIT`:

```
startCount: 3        bandits at room start
spawnEveryMs: 15000  one more every 15s
maxAlive: 12         cap
fireDelay: 1750      ms between shots
bulletDamage: 10     per hit
fireRange: 38        metres
respawnMs: 2600      bandit comes back
```

ב-`boss.js`, אובייקט `BOSS`:

```
firstBossMs / nextBossMs: 90000    one every 90s, next 90s after a death
keepDistance: 9      closer than a bandit's 11
radius: 0.85         and wider
chargeSpeed / chargeDamage: 14 m/s, 22 on contact
slamRadius / slamDamage: 6.5 m, 24 flat
tnt: 38 damage, 7.5 m, falls off with distance
poison cloud: 6 damage every 0.45 s for 5.2 s inside 4.2 m
blinkMin / blinkMax: 8, 26
```

`BOSS_TYPES` — חיים, קצב אש ומהירות לכל אחד מהשמונה. `GUN` — כמה כדורים,
כמה פיזור, כמה נזק ובאיזו מהירות לכל יכולת.

ב-`server.js`: `RESPAWN_MS` (שחקן, 4000), `WEAPONS` (טבלת הנזק),
`SPAWNS` (12 נקודות מאומתות), `ROOM_LIMIT` (8), `MODES`.

בלקוח: `INTERP_DELAY` (110), `NET_SEND_HZ` (20), `MAP_SEED`.

**עמידה במקום בשטח פתוח = מוות תוך ~10 שניות.** נמדד.
**עמידה במקום מול בוס = 600 עד 2300 נזק לדקה, תלוי מי הגיע.** גם נמדד.

---

## 9. פרוטוקול הבוס (מה עובר על החוט)

מהשרת ללקוח:

| אירוע | מתי | תוכן |
|---|---|---|
| `boss-spawn` | פעם אחת, בכניסה | `{t, hp, x, z}` — `t` הוא אינדקס ב-`BOSS_TYPES` |
| `boss` | 10 בשנייה כשהוא חי, פעם בשנייה כשלא | `{t, hp, b:[x,z,yaw,health,ghost,charge]}` או `{in: שניות}` |
| `boss-shot` | לכל קליע | `{k: סוג היכולת, o, d}` |
| `boss-hazard` | זריקת דינמיט/רעל | `{i, k, o, v, f}` |
| `boss-boom` | הפיצוץ | `{i, k, p}` |
| `boss-slam` / `boss-blink` / `boss-roar` | יכולות | `{p, k}` |
| `boss-died` | מוות | `{by, t, x, z, headshot, nextIn}` |

מהלקוח לשרת: `boss-hit` עם `{w, body, head}` בלבד — נשק ומספר כדוריות.
כל השאר נקבע בשרת.

**חבילת ה-`boss` היא מקור האמת.** מי שנכנס לחדר באמצע קרב בונה ממנה את
הבוס לבד, בלי לראות `boss-spawn` — ולכן היא נושאת גם `t` וגם `hp`.

---

## 10. הצעד הבא — שלב 13

להעביר את הגלים לשרת. כרגע מונה ה-`wave` בלקוח מתקדם כשהבוס נופל — וזה
נשאר מסונכרן רק כי כולם רואים את אותו מוות. זה עובד, וזה שביר: מי שנכנס
באמצע מתחיל מגל 1 בזמן שהאחרים בגל 4.

שלב 13: `room.wave` בשרת, טיימר 45 שניות משלו, שידור המספר בחבילת ה-`boss`
(או חבילה משלו), והלקוח רק מקריא. גם תגמול התחמושת של כל גל עובר לשם.

---

## 11. איך לפתוח את השיחה הבאה

> ממשיכים את פרויקט Wild West Multiplayer. מצורף HANDOFF.md עם כל ההקשר.
> הקבצים נמצאים ב-`C:\Users\tamip\Desktop\CLOUD ME\משחק\`.
> תקרא את המסמך ואת הקבצים, ותתחיל בשלב 13 — העברת הגלים לשרת.

שיטת העבודה שעבדה: **שלב אחד בכל פעם, נבדק מקומית לפני שמעלים, ובסוף
נאמר במפורש אילו קבצים להעלות.** אני מעלה ידנית ל-GitHub ומחכה ל-deploy.
