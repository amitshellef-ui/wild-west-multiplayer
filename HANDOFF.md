# Wild West Multiplayer — מסמך המשכיות

מסמך זה נכתב כדי שאפשר יהיה להמשיך את הפרויקט בצ'אט חדש בלי לאבד הקשר.
**העלה אותו בתחילת השיחה הבאה.**

---

## 1. מה זה

משחק FPS בדפדפן בסגנון מערב פרוע (Three.js r128, קובץ HTML יחיד), שהופך
בהדרגה למולטיפלייר אמיתי דרך Node + Express + Socket.IO.

| | |
|---|---|
| Repository | `amitshellef-ui/wild-west-multiplayer` (GitHub, העלאה ידנית דרך הדפדפן) |
| אירוח | Render — build `npm install`, start `npm start` |
| כתובת | https://wild-west-multiplayer.onrender.com |
| עבודה מקומית | `C:\Users\tamip\Desktop\CLOUD ME\משחק\` |

Render ב-Free tier נכבה אחרי חוסר פעילות — החיבור הראשון אחרי הפסקה לוקח
30-50 שניות. זו לא תקלה.

---

## 2. איפה אנחנו עומדים

**שלבים 1-24 עלו ואומתו בשרת החי** (2026-09-16). כל 9 הקבצים ב-Render זהים
בבייטים לעותק המקומי, ו-`tests/live-steps20-24.js` עבר 20/20 מול השרת החי.

> בתיקייה `backup-step23/` יש עותק של כל הקבצים במצב של סוף שלב 23 — בדוק
> ועובד. אפשר להעלות אותו קודם כשלב נפרד (20-23), ורק אחריו את 24.
> בדיקות: תיקיית `tests/` (לא להעלות אותה).

| שלב | מה | סטטוס |
|---|---|---|
| 1 | חיבור Socket.IO | ✅ |
| 2 | זיהוי שחקנים מחוברים | ✅ |
| 3+4 | סנכרון מיקום וכיוון (אוחדו) | ✅ |
| 5 | גופים לשחקנים אחרים | ✅ |
| 6 | תנועה חלקה (אינטרפולציה + הליכה) | ✅ |
| 7 | סנכרון ירי | ✅ |
| 8+9 | נזק וחיים עם סמכות שרת (אוחדו) | ✅ |
| 10 | מוות והחייאה | ✅ |
| 14-16 | חדרים, קוד, הצטרפות (אוחדו, **הוקדמו**) | ✅ |
| 11א | מפה דטרמיניסטית + ניווט בשרת | ✅ |
| 11ב | בנדיטים משותפים בשרת | ✅ |
| 11ג | ירי בנדיטים בשרת | ✅ |
| 12 | בוס לשרת | ✅ בשרת החי |
| 13 | גלים לשרת + עקומת קושי | ✅ בשרת החי |
| 14 | מגבלות חדר + כיווץ תעבורה | ✅ בשרת החי |
| 15 | קו ראייה בשרת | ✅ בשרת החי |
| 16 | לוח תוצאות (Tab) | ✅ בשרת החי |
| 17 | קצב תגבורת לפי גל | ❌ לא נבנה — נמדד, לא היה משנה כלום. הוחלף בשלב 19 |
| 18 | ניווט: אזורים, תקציב A*, חימום | ✅ בשרת החי |
| 19 | קושי לפי גל: חזרה, ירי, דיוק, חיים | ✅ בשרת החי |
| 20 | תיבות התנגשות לסלעים הגדולים + אינדקס מרחבי | ✅ בשרת החי |
| 21 | שלב 2 לכל בוס מתחת ל-30% | ✅ בשרת החי |
| 22 | חיבור מחדש — 20 שניות לחזור לאותו מקום | ✅ בשרת החי |
| 23 | החייאה — חבר מחזיק E חמש שניות | ✅ בשרת החי |
| 24 | משימות — בנק, עגלה, שטח | ✅ בשרת החי |

**אומת בשרת החי:** כדור דרך רצועת סלע נדחה ובשטח פתוח נקלט (37); 0 מתוך ~5,700
מיקומי בנדיט בתוך קיר או סלע; חיבור מחדש — אותו id, אותו חדר, אסימון לא דלף;
נפילה, אף אחד לא קם לבד, החייאה אחרי 5.1 שנ' עם 50; גל 2 ב-45.0 שנ' עם בנק, מצטרף
מאוחר מקבל את המשימה, הבנק ירד ל-140-230 ושרד 40 שנ', ‎+25 חיים (60 → 85), ~1.4
חבילות בשנייה; בוס ב-90.0 שנ', שלב 2 אחרי 6 פגיעות (180/900) עם `phase: 1`.

**מה שנשאר פתוח:** המכרה והמכלאה החסומים לבנדיטים — ראה סעיף 10.

**שלבים 25-29 — מודלים לדמויות (בעבודה מ-2026-09-18).** הכל כתוב ב-**`PLAN-MODELS.md`** —
לקרוא אותו יחד עם המסמך הזה. בקצרה: 25א (מודל החייל בבלנדר) ✅; 25ב (`models/` בריפו, נטען
מ-jsDelivr, commit `d0d2256`) ✅ ועלה; 26א (הבנדיטים ו-8 הבוסים = קאובוי מאונפש, רק
`wild-west-fps-arsenal.html`) נבדק מקומית — פגיעות 138/138, גיבוי לקוביות, זיכרון, אופליין, FPS.
החלטות עיקריות: לבנדיטים-מודלים אין צל אמיתי (צל עגול רך עד 15 מ'), לבוסים זוהר בצבע העיניים,
והחלפת מודל לבוס בעתיד = שורה בטבלה `BOSS_LOOKS` + רשומה ב-`MODEL_SPECS`. בדיקות: `tests/models.html`,
`tests/bench-models.html` (לא עולים).
**26ב (החייל לשחקנים האחרים, רק `wild-west-fps-arsenal.html`, בלי שרת) — נכתב ונבדק מקומית
(2026-09-18).** פירוט, מדידות והחלטות ב-`PLAN-MODELS.md` סעיף 6, שלב 26ב. גיבוי של ה-HTML במצב
26א: `backup-step26a/`. ✅ עלה (2026-09-18).
**26ג (טעינה נראית + תיקון מצטרף באמצע החייאה, `server.js` + ה-HTML) — ✅ עלה ואומת בשרת החי
(2026-09-18).** כל 9 הקבצים ב-Render זהים בבייטים למקומיים; `tests/live-step26c.js` 23/23 מול Render;
בדפדפן (אותו HTML, מחובר ל-Render — חלונית Claude לא מורשית לפתוח את כתובת Render ישירות): מצטרף
1.5 שנ' לתוך החייאה רואה מחייה כורע והפס ממשיך מ-1.54 שנ'; טעינה 3.2 שנ' עם רגליים בהליכה; ירייה
קוטעת טעינה; הטעינה של הדף מגיעה לאחרים (3200ms). הערה: בחדר חי הבנדיטים יורים — שחקן מתוסרט
שעומד במקום נופל תוך שניות. גיבוי של 26ב: `backup-step26b/`.
**27 (הרובוט = בוס 3, `boss.js` + ה-HTML) — ✅ עלה ואומת בשרת החי (2026-09-18):** 9 הקבצים זהים בבייטים; `tests/live-step27.js` 14/14 בחדר פרטי — בוס 1 ו-2 הופלו, השלישי הרובוט, צרורים של 2.42 שנ' / 28 כדורים, 0 תזוזות ב-120 חבילות אש, רמיסה, OVERDRIVE עם קיטור, הופל, והבא הצלפית. פירוט ומדידות ב-
`PLAN-MODELS.md` סעיף 6 שלב 27. **9 בוסים** עד שלב 28 (הרובוט נכנס שלישי, אחרי charge — הסדר הוא
פרוטוקול: `boss.js` וה-HTML עולים יחד). גיבוי של 26ג: `backup-step26c/`. **פתוח להחלטה שלך:** הרובוט
מסוכן רק למי שעומד מולו (נמדד — ראה PLAN).
**28 (הדרקון = בוס 10 והאחרון, וסוף המשחק; `boss.js` + `server.js` + ה-HTML) — ✅ עלה ואומת בשרת החי
(2026-09-18):** 9 הקבצים זהים בבייטים, `tests/live-step28.js` 19/19. הדרקון (2400 חיים, רדיוס 1.3, שומר מרחק 12) נושף כדורי אש שנוחתים בדיוק איפה
שעמדת ומשאירים שלולית אש (4.2 מ', 8 כל 0.45 שנ', 4 שנ'), ועט עליך (30) כל 6 שנ'; בשלב 2 — 4 בנדיטים, שני
כדורים במניפה ועיטה כל 3.5 שנ'. כשהוא נופל: `room-won` עם הטבלה הסופית, החדר קופא (כל הבנדיטים נופלים, אין
גל, אין בוס), מסך VICTORY עם כפתור "משחק חדש" (`room-restart` → `room-restarted` + התחייה לכולם, גל 1).
אופליין — אותו מסך. פירוט ומדידות ב-`PLAN-MODELS.md` סעיף 6 שלב 28. גיבוי של 27: `backup-step27/`.
**30א (השלד = בוס 1 במקום BLACK-JACK McCREADY; `boss.js` + ה-HTML + `models/skeleton.glb`) — ✅ עלה ואומת
בשרת החי (2026-09-19):** המודל ב-commit `08a3cfa` (jsDelivr, `MODEL_BASE_CDN` נעול אליו), הקוד זהה בבייטים
ב-GitHub וב-Render, `tests/live-step30a.js` 8/8 (בוס 1 ב-90 שנ', כדור אחד לשליפה, 24 לפגיעה, הופל, הבא בוס 2).
**30ב (דו-קרב בצהריים — "HIGH NOON"; `boss.js` + `server.js` + ה-HTML) — ✅ עלה ואומת בשרת החי
(2026-09-19):** commit `de66cca`, ארבעת הקבצים זהים בבייטים ב-Render, `tests/live-step30b.js` 8/8. כל 15 שנ' (הראשון 6 שנ' אחרי שהגיע) השלד מסמן שחקן אקראי שהוא רואה עד 32 מ': גולגולת מעל הראש
של המסומן אצל כולם, ספירה של 3 שנ' על המסך, השמיים מחשיכים, תקתוק שעון; הוא עומד עם יד מעל הנרתיק ולא יורה.
בסוף — ירייה אחת של 80 אם יש לו קו ראייה, החטאה אם המסומן מאחורי מחסה. ירייה בגולגולת (headshot) של כל
שחקן בזמן הספירה שוברת את הדו-קרב: אין ירייה, והוא על ברך 2 שנ'. `SKELETON` ב-`boss.js` = `BOSS_SKELETON`
ב-HTML (נבדק ב-`t9-tables`). אירוע חדש `boss-duel` וקצה חדש בשרת (`boss.duelHeadshot` מתוך `boss-hit`) — ראה
סעיף 9. אופליין אין דו-קרב. גיבוי: `backup-step30b/` (= 30א החי).
**30ג (התפרקות — "BONE SCATTER"; `boss.js` + `server.js` + ה-HTML) — ✅ עלה ואומת בשרת החי (2026-09-19):**
commit `d3afcd0`, `tests/live-step30c.js` 9/9. **תוספת — זינוק (`boss.js` + ה-HTML) — ✅ עלה ואומת (commit `963ed72`,
`live-step30c` 10/10):** לבקשתך, בזמן המכה הוא מזנק אל השחקן שבא אליו (16 מ'/ש', עד 1.2 מ' ממנו) עד שהמכה נוחתת — מי
שעומד או זז הצידה / מסתובב נפגע, מי שבורח מיד כשהוא קם (ריצה) יוצא מהטווח. (לפני התוספת: עומד 40/40,
זז 0/40.) פעם אחת, כשהשלד יורד לחצי חיים: קורס לערימה (1 שנ', אפשר עדיין לפגוע בו), נעלם 2 שנ' (אין גוף,
אין פגיעות — `boss.hurt` מחזיר `null`; העצמות עפות באוויר אל השחקן שנבחר, וקרקוש מאחוריו — אצל עצמך מעומעם
כשהוא מאחורייך), מתחבר (Reassemble, 1.25 שנ') 2.4 מ' **מאחורי** שחקן אקראי — לפי הכיוון שהוא מסתכל ברגע
ההופעה (הצידה אם יש קיר מאחוריו, מלפנים רק כשהוא סגור מכל הכיוונים) — ומכה (Melee) 30 בכל מי שעד 3.2 מ'
לפניו, 0.375 שנ' לתוך המכה (ואחרי התוספת — מזנק אליו בדרך). דו-קרב שרץ מתבטל, והבא לא לפני 5 שנ' אחרי הסוף. `SKELETON` קיבל את המספרים
(= `BOSS_SKELETON`, נבדק ב-`t9-tables`; התוספת: `lungeSpeed 16`, `lungeStop 1.2`). אירוע חדש `boss-scatter` ושדות 8-10 ב-snapshot עם 3-6 — ראה
סעיף 9. אופליין אין התפרקות. גיבוי: `backup-step30c/` (= 30ב החי), `backup-step30c-lunge/` (= 30ג החי).
**30ד (קציר עצמות — "BONE HARVEST"; `boss.js` + `server.js` + ה-HTML) — ✅ עלה ואומת בשרת החי (2026-09-19):**
commit `7e1fe32`, `tests/live-step30d.js` 8/8 מול Render — גם קפיצה בדיוק ברגע שהטבעת נוגעת על המסך עוברת.
**⚠ באג שנמצא אחרי ההעלאה ותוקן (2026-09-19; התיקון עלה ב-commit `4777ffa`):** הפונקציה שמציירת טבעת קציר נקראה `makeRing` — אותו שם כמו
הטבעת של המשימות (שלב 24). השנייה דרסה את הראשונה: כל משימה (בנק / עגלה / החזקה) זרקה שגיאה בכל פריים, והפריים לא
צויר. עכשיו `makeHarvestRing`; נבדק — 8 משימות, 0 שגיאות, הקציר עובד. `t9-tables` בודק מעכשיו שאין שם שמוגדר פעמיים
בדף (תופס את הגרסה שבאוויר). 14 שנ' אחרי שהשלד הגיע ואז כל 22 שנ' (כשיש מישהו שהוא רואה עד 22 מ'): Summon (1.5 שנ'), שלושה
סיבובים של שנייה (SpinLow / SpinHigh) — וכל סיבוב שולח טבעת עצמות שמתרחבת על הקרקע ב-9 מ'/ש' עד 24 מ':
**נמוכה, גבוהה, נמוכה**. נמוכה (בגובה השוקיים) — לקפוץ מעליה; גבוהה (מעל ראש של מי שעומד) — להישאר על
הקרקע, קפיצה מכניסה את הראש לתוכה. כל טבעת שתופסת — 25; קיר עוצר אותה. אין התכופפות במשחק, כך שהקפיצה
היא כל התשובה. השרת שופט לפי גובה הרגליים ששולח הלקוח (y פחות 1.72): נמוכה עוברת אם באחת הדגימות 50-250ms
אחרי שהטבעת הגיעה הרגליים מעל 30 ס"מ; גבוהה — אם באחת מהן הרגליים מתחת ל-12 ס"מ (החלון מכסה את ההשהיה
של הרשת; הקפיצה במשחק: 6.1 מ'/ש', כבידה 21 — 0.47 שנ' מעל 30 ס"מ). ואז Exhausted 3 שנ' — עומד, לא יורה:
החלון לפגוע בו. אף פעם לא יחד עם דו-קרב (3 שנ' לפחות ביניהם); התפרקות מבטלת קציר, טבעות שכבר יצאו
ממשיכות. בלקוח: הטבעות (פס שקוף + 44 עצמות מסתובבות, `InstancedMesh`), מצוירות 100ms קדימה
(`RING_LEAD_MS`), וכשטבעת עומדת להגיע אליך — "⬆ JUMP ⬆" / "⬇ STAY DOWN ⬇" במרכז המסך. `SKELETON` =
`BOSS_SKELETON` (נבדק ב-`t9-tables`). אירוע חדש `boss-harvest`, snapshot 7-9 — ראה סעיף 9. אופליין אין
קציר. גיבוי: `backup-step30d/` (= 30ג+זינוק החי). **אחרי 30ד השלד גמור;** הבא — שלב 29 (ליטוש) או בוס חדש. השורה הראשונה בטבלה היא עכשיו `skeleton` — "BONES McCREADY", 900 חיים,
מהירות 3.0, אקדח אחד לשליפה (24 נזק, כל 1.1 שנ'), מודל משלו ×1.6 עם אקדח בנרתיק שנשלף בכל ירייה. הצרור
המשולש (`burst`, כולל `burstShots` של שלב 2) נמחק משני הצדדים. היכולות מהמסמך "שלד קאבוי" — דו-קרב
בצהריים, התפרקות (פעם אחת, ב-50%), קציר עצמות (הגל הגבוה בגובה קפיצה, אין התכופפות) — בשלבים 30ב-30ד.
פירוט ב-`PLAN-MODELS.md` סעיף 6 שלב 30. גיבוי: `backup-step30a/`.
**31 (הרוח — בוס 7, GHOST-WALKER COLE, מקבל מודל משלו ואחר כך שלוש יכולות מהמסמך "רוח").** התוכנית ב-**`PLAN-GHOST.md`**
(לא עולה). **31-0 (בלנדר) ✅:** `models/ghost.glb` — 3.80MB, 50,000 משולשים, 25 עצמות בלי רגליים (מרחפת 0.20 מ' — אפוי
בקליפים), אקדח משלו תמיד ביד ימין (`gun` / `muzzle`), מעיל וזנב עשן עם תנועה משנית (`wisp*`, `coat*`), 12 קליפים —
Idle, Walk, Run, Shoot, Hit, Death, Vanish, Appear ועוד ארבעה ליכולות (SpectralShot, GraveCast, Dash, Recover). מדידות
ותזמונים ב-`models/README.md` סעיף ghost. **31א (המודל על בוס 7 — רק ה-HTML + `models/`, השרת לא השתנה) — עלה
(2026-09-19):** `models/ghost.glb` + `models/README.md` ב-commit `d8a7d5e` (jsDelivr — כל 6 המודלים זהים בבייטים),
ה-HTML + המסמך הזה ב-`b94ce14` — עוד לפני שהכתובת ננעלה, אז `ghost.glb` הגיע מ-Render (`/models/`, הגיבוי); **הנעילה של
`MODEL_BASE_CDN` ל-`d8a7d5e` עלתה אחר כך** (commit `bdbf64c`, ה-HTML + המסמך הזה) — כל 12 הקבצים ב-Render זהים בבייטים.
**אומת בשרת החי:** `tests/live-step31a.js` — בוסים 1-6 הופלו, בוס 7 הוא הרוח (800), קפיצות של 18.7 ו-23.9 מ' (בטווח
8-26), שקופה אחרי קפיצה, פגיעות נקלטות בזמן שקיפות, הופלה, והבא בוס 8. (בדיקה אחת נכשלה בגלל הבדיקה עצמה: היא ציפתה
לשלוש קפיצות, אבל הצלפים בזמן השקיפות הפילו את הרוח תוך 7.6 שנ'; תוקן לשתיים.) פירוט: `MODEL_SPECS.ghost` + `BOSS_LOOKS.ghost` (×1.45, הראש ב-~2.8 מ'), `MODEL_LOWER` כולל את
עצמות המעיל והזנב (אחרת כל ירייה מקפיאה אותם), בלי צל (`noShadow`). הזוהר של הטקסטורה (עיניים, ידיים, זנב) נשאר בצבעים
שלו — `glowMap` חדש ב-`BOSS_LOOKS`; בלעדיו צבע העיניים היה מעמעם אותו ל-8% וצובע את האקדח סגול; בשלב 2 הוא בוער באדום
(`emissiveRest`, גם בהבהוב הפגיעה אופליין). VANISH: אותה שקיפות 22% כמו היום (הוחלט לפי תמונות), **והיא חוזרת לאטומה**
(קודם `transparent` נשאר דלוק לתמיד); ערפל ולחישה במקום שעזבה, והיא נפרשת (Appear) במקום שהופיעה. מוות: Death, מתפוגגת
ונשאבת לאדמה עם ערפל, נעלמת תוך 1.2 שנ'. נבדק: `t9-tables`, `t8` 10/10, `t29-phase2` 16/16; בדפדפן (`tests/run31.js`) —
פגיעות 63/63 דרך התיבות (45 רגיל, 18 בזמן שקיפות), ריצה, ירייה, VANISH, שלב 2, מוות, `?cubes`, בלי שגיאות. FPS אצלך ב-Chrome
(`tests/bench-ghost.html`): תנועה 22.0, VANISH 23.2, עמידה 19.1, מוות 21.6 — **אבל בפעם הראשונה שהרוח על המסך ~5 שנ' של
קפיצות** (בניית ה-shader והטקסטורות; הכי ארוך 2.9 שנ'). הוצע "חימום" בטעינה — **הוחלט להשאיר.** גיבוי: `backup-step31a/`
(= המצב החי של 30ד).
**31ב (💀 SPECTRAL SHOT — קליע רפאים שעובר דרך קירות; `boss.js` + `bandits.js` + `server.js` + ה-HTML) — נכתב ונבדק
מקומית (2026-09-19), עוד לא עלה.** לצד האקדח הרגיל וה-VANISH (שנשאר עד 31ד): כל 7 שנ' (הראשון 5 שנ' אחרי שהגיעה; בשלב 2
כל 5) הרוח בוחרת את השחקן **הקרוב ביותר עד 45 מ' — גם בלי קו ראייה**, נעצרת, מרימה את האקדח בשתי ידיים (250ms), והאקדח
**זוהר 600ms** — האזהרה, שהלקוח מצייר **דרך קירות** (sprite בלי בדיקת עומק, גדל עם המרחק כדי לא להפוך לנקודה) עם יללה
עולה וכתובית "☠ SPECTRAL SHOT — MOVE ☠" למי שמכוונים אליו. היא עוקבת אחרי המטרה עד **250ms לפני הירי** ואז הכיוון ננעל
(ההבזק הופך לבן ומהבהב): מי שזז בזמן ההבזק — הקליע עובר לידו; מי שעומד (או יושב מאחורי קיר) — **60**. קליע אחד ב-60 מ'/ש',
**לא נעצר בקירות** (דגל `thru` על הקליע — `stepBullets` ב-`bandits.js` מדלג על `blockedAt`, אז הוא גם לא פוגע בקיר הבנק),
נעלם אחרי 50 מ' (`maxLife`) או באדמה. אחר כך מורידה את האקדח (500ms). בכל הזמן הזה: לא זזה, האקדח הרגיל שותק, ו-VANISH
מחכה. מטרה שנפלה בזמן הכיוון → עוברת לקרוב הבא (אותה ירייה, באותו זמן); אין אף אחד → האקדח יורד בלי ירייה; הרוח מתה
בזמן הכיוון → הקליע לא יוצא. `GHOST` ב-`boss.js` = `BOSS_GHOST` ב-HTML (נבדק ב-`t9-tables`). אירוע חדש `boss-spectral`,
`boss-shot` עם `k: "spectral"`, וקוד 11 ב-snapshot — סעיף 9 (**הקודים של הרוח 11-19; 1-9 של השלד**). בלקוח: קליפ
`SpectralShot`, הקליע = כדור סגול עם הילה ושובל שנראה דרך קירות ולא נעצר בהם (כדור רגיל כן — `updateBanditBullets`),
היבהוב על כל קיר שהוא חוצה, שריקה כשהוא עובר ליד. אופליין אין. נבדק: `tests/t42-spectral.js` 30/30 ×3, כל החבילה ירוקה,
`tests/live-step31b.js` מול שרת מקומי 10/10 (סוקט ששולח מיקום 20 פעם בשנייה: עומד = 60, מאחורי קיר בלי קו ראייה = 60 ×3,
הולך הצידה מרגע שהאקדח עולה = 0 מתוך 3); בדפדפן (`tests/run31.js` — `/spectral`, `/wall`, `GHOST_SPEC_HOLD`, `GHOST_GOD`):
ההבזק על הלוע, ההבזק והקליע נראים דרך קיר מ-15 מ', מצטרף מאוחר (מה-snapshot בלבד), מוות באמצע הכיוון (Death מתנגן,
ההבזק כבה, אין קליע), `?cubes`, בלי שגיאות. **נזק ב-60 שנ' (`tests/m42-spectral-dpm.js`, ממוצע 5 ריצות; לפני → אחרי):**
עומד 678 → 1050 · הולך הלוך-חזור 258 → 273 · צעד-צעד לצדדים 273 → 243 · מקיף 39 → 135 · **מסתתר מאחורי קיר 0 → 480**
(40 מתוך 40 פוגעים). גיבוי: `backup-step31b/` (= 32 החי). **✅ עלה ואומת בשרת החי (2026-09-19):** commit `9715c71`,
5 הקבצים (`boss.js`, `bandits.js`, `server.js`, ה-HTML, `HANDOFF.md`) זהים בייט-בייט ב-Render, ו-`tests/live-step31b.js`
מול Render 11/11 (בוסים 1-6 נפלו, הרוח 800; עומד = 60 ×2 ב-851/864ms; מאחורי קיר = 60 ×2; הולך הצידה = 0 מתוך 3; בוס 8 הבא).
**31ג (🪦 GRAVE BURST — ידיים מהאדמה; `boss.js` + `server.js` (שורה אחת — האירוע החדש) + ה-HTML) — נכתב ונבדק מקומית
(2026-09-19), עוד לא עלה.** כל 13 שנ' (הראשון 10 שנ' אחרי שהגיעה) הרוח בוחרת **שחקן אקראי חי עד 30 מ' — בלי צורך בקו
ראייה**, נעצרת ומרימה יד שמאל (500ms, קליף `GraveCast`), ו**עיגול ברדיוס 2.5 מ' נפתח על הקרקע בדיוק איפה שהוא עומד
באותו רגע**. העיגול מתמלא 1100ms — ואז הידיים פורצות: **40 לכל מי שבתוך העיגול ועל הקרקע**, לא רק למטרה. ההתחמקות: לצאת
מהעיגול או לקפוץ. **נשפט כמו הטבעת הנמוכה של השלד** — 250ms אחרי הפריצה, לפי כל הדגימות של מיקום וגובה הרגליים בין 50
ל-250ms אחריה: דגימה אחת מחוץ לעיגול או עם רגליים מעל 30 ס"מ = ניצל (אותם מספרים של `SKELETON`, בלי חדשים). בזמן הזה: לא
זזה, האקדח הרגיל שותק, VANISH מחכה. **Spectral ו-Grave אף פעם לא יחד** — השני מחכה 2.5 שנ' (`gapMs`) אחרי סוף הראשון.
מטרה שנפלה בזמן שהיד עולה → מישהו אחר; אין אף אחד → היד יורדת בלי עיגול (`done, off`). הרוח מתה בזמן המילוי → אין פריצה.
שחקן שנפל (downed) לא סופג. אירוע חדש `boss-grave` וקודים 12-13 ב-snapshot (13 עם מיקום העיגול, למצטרף באמצע) — סעיף 9.
בלקוח: דיסק כהה שמתכהה + טבעת סגולה בשוליים + טבעת בהירה שנסגרת מהשוליים למרכז עד רגע הפריצה, ערפל ורעש אדמה בזמן
המילוי, "⚠ MOVE ⚠" במרכז המסך למי שעומד בעיגול; בפריצה 10 ידיים-עצמות סגולות (`InstancedMesh` אחד, גיאומטריה ממוזגת של
אמה + כף + 4 אצבעות + אגודל — `mergeGraveGeos`) עולות ב-0.15 שנ', נשארות 0.25 ושוקעות ב-0.6, אבק, ערפל, רעד מסך; שורה
בפיד "THE GRAVE CAUGHT YOU" / "OUT OF THE GRAVE". כל פונקציה בשם ייחודי (`makeGraveCircle`...). אופליין אין. נבדק:
`tests/t43-grave.js` 40/40 ×3 (כולל: יציאה בצעד שהשרת רואה 50-250ms אחרי = ניצל, 400ms = נפגע; קפיצה שמגיעה עד 250ms
באיחור = ניצלה; קפץ מוקדם ונחת = נפגע; שני שחקנים בעיגול = שניהם; 90 שנ' עם Spectral ו-VANISH — אף פעם לא יחד, פער
מינימלי 2.5 שנ'), `t42` ו-`t29` מכבים את ה-Grave (כמו שהם מכבים את Spectral), כל החבילה ירוקה (חוץ מ-`t37` "keeps its
distance" / "swoops", הפכפכות — עוברות בהרצה חוזרת), `tests/live-step31c.js` מול שרת מקומי 12/12 (סוקט עם מיקום וגובה
ב-20Hz: עומד = 40 ×2, מאחורי קיר = 40 ×2, הולך החוצה = 0/2, **קופץ כשהפריצה על המסך = 0/3**); בדפדפן (`tests/run31.js` —
`/grave`, `GHOST_GRAVE_HOLD`): הרצף המלא, "MOVE" רק בזמן המילוי ורק בפנים, מצטרף באמצע (12 ו-13 מה-snapshot, הקליף
מצטרף בנקודה הנכונה), מוות באמצע המילוי (אין פריצה, העיגול נעלם, Death מתנגן), בלי שגיאות. **נזק ב-60 שנ'
(`tests/m43-grave-dpm.js`, ממוצע 5; 31ב → 31ג):** עומד 987 → 994 · הולך הלוך-חזור 339 → 310 · צעד-צעד לצדדים 270 → 305 ·
מקיף 186 → 171 · מסתתר מאחורי קיר 480 → 580 · **קופץ בלי הפסקה 777 → 688 (רק 5 מ-20 פריצות פוגעות)**. העומד כמעט לא
משלם יותר — הזמן שהיא מטילה הוא זמן שהאקדח הרגיל שותק. גיבוי: `backup-step31c/` (= 31ב החי).
**✅ עלה ואומת בשרת החי (2026-09-19):** commit `65deba2`, 4 הקבצים זהים בייט-בייט ב-GitHub וב-Render (כל 10 קבצי המשחק
זהים), ו-`tests/live-step31c.js` מול Render 13/13 (בוסים 1-6 נפלו; יד 507/498ms, מילוי 1096/1103ms, העיגול 0.00 מ' מתחתינו,
עומד = 40 ×2; מאחורי קיר = 40 ×2; יוצא = 0/2; **קופץ = 0/3 מעל רשת אמיתית**; בוס 8 הבא).
**31ד (👻 GHOST DASH — מתפוגגת, נעלמת ופורצת לידך; `boss.js` + `server.js` (שורה אחת — האירוע החדש) + ה-HTML) —
נכתב ונבדק מקומית (2026-09-20), עוד לא עלה.** ה-Ghost Dash **מחליף את VANISH אונליין** (החלטה 1
ב-`PLAN-GHOST.md`). כל 12 שנ' (הראשון 8 שנ' אחרי שהגיעה) הרוח בוחרת שחקן אקראי חי עד 40 מ' — בלי צורך בקו ראייה — ורצה:
**ערפל** (500ms, קליף `Vanish`, שקופה ועדיין אפשר לפגוע בה) → **נעלמת** (1000ms, **אין גוף בכלל** — `boss.hurt` מחזיר `null`
כמו בהתפרקות השלד, והיא יורדת מרשימת המטרות ומהמפה הקטנה); **700ms לפני שהיא יוצאת** נפתחת **מערבולת ערפל
על הקרקע** בדיוק במקום שבו תצא, עם לחישה — האזהרה → **יוצאת** (450ms, `Appear`, **7 מ' ממך בזווית אקראית** — לא
דווקא מאחור, זה של השלד — במקום שהגוף נכנס בו ויש ממנו קו ישר אליך) → **הסתערות** (16 מ'/ש', עד 12 מ', עד 750ms,
נעצרת בקיר; **35** לכל מי שעד 1.8 מ' מהקו שלה, פעם אחת לשחקן) → **התאוששות** (1000ms, `Recover` — עומדת, לא
יורהת: חלון הנזק). **הכיוון ננעל בסוף היציאה**, על איפה שאתה אז — ולכן **צעד הצידה מרגע שהיא יוצאת מוציא
אותך מהקו**; עמידה במקום = פגיעה ודאית; להתרחק בקו ישר לא עוזר (היא מהירה ממך). בשלב 2: **שתי הסתערויות
ברצף** — אחרי הראשונה ערפל קצר (400ms), נעלמת קצר (400ms), סימן חדש על **מטרה חדשה**, ורק אז התאוששות.
מטרה שנפלה / עזבה בזמן שהיא נעלמת → היא יוצאת איפה שנכנסה, בלי הסתערות (`done, off`). במקום צר מדי לגוף שלה
(פתח בין שני בתים) — אותו דבר; נמדד: ~5-15% מהפעמים במשחק רגיל. **אף פעם לא יחד עם Spectral או Grave** — השני
מחכה 2.5 שנ' (`gapMs`); בזמן הרצף האקדח הרגיל שותק והיא לא הולכת (חוץ מההסתערות עצמה).
**מה ירד מהשרת:** `BOSS.ghostEveryMs` / `ghostForMs` / `blinkMin` / `blinkMax`, `PHASE2.ghostEveryMs`, `b.ghostUntil`,
והאירוע `boss-blink` (השדה `blinks` נשאר ב-`emptySink` ותמיד ריק). **אופליין ה-VANISH נשאר כמו שהוא** —
המוח המקומי שלם ב-HTML ולא עולה לשרת כלום, ובלעדיו לבוס 7 במשחק היחיד אין יכולת בכלל; התווית בפס
הבוס היא "GHOST DASH" בשניהם. אירוע חדש `boss-dash` וקודים 14-18 ב-snapshot (15 נושא את נקודת המערבולת
למצטרף באמצע) — סעיף 9. בלקוח: הקליפים `Vanish` / `Appear` / `Dash` (לולאה) / `Recover`, מערבולת עם
שתי טבעות שמסתובבות הפוך זו לזו וגדלה עד הרגע (`makeDashWhirl`, נסגרת ב-260ms כשהיא יוצאת), שובל ערפל
בהסתערות, "⚠ STEP ASIDE ⚠" למי שהסימן עליו, ושורות בפיד. **נבדק:** `tests/t44-dash.js` 42/42 ×3,
`t29-phase2` 17/17, `t42` 30/30, `t43` 41/41, `t9-tables` וכל החבילה; `tests/live-step31d.js` מול שרת מקומי
12/12 (סוקט ששולח מיקום ב-20Hz: ערפל 510-558ms, נעלמת 1000-1043, סימן 661-712 לפני היציאה ובאותה נקודה
בדיוק, 7.00 מ' ממני, עומד = 35 ×2, **יריות בזמן שהיא נעלמת = 0 נזק, ורגע אחרי כן 440**); בדפדפן
(`tests/run31.js` — `/dash`, `GHOST_HOLD`): הרצף המלא עם כל הקליפים, הגוף נעלם וחוזר, המערבולת גדלה ונסגרת,
ה-HUD, מצטרף מאוחר (מה-snapshot בלבד — הכול נבנה מחדש), מות באמצע ההסתערות (Death מתנגן, הכל נקה),
שלב 2 = שתי הסתערויות ואז התאוששות, `?cubes`, בלי שגיאות. **טבלת ההתחמקות (`tests/m44-dash-dodge.js`,
40 הסתערויות לשורה, בשטח פתוח, 12 / 16 / 20 מ'/ש'):** עומד 41/41 · 41/41 · 41/41 · צעד הצידה 0/41 · 1/41 · 3/40 ·
רץ הצידה 1/40 · 1/40 · 3/40 · מתרחק בקו ישר 5/41 · **40/41** · 39/41 · בורח בריצה 12/41 · 23/41 · 17/40 · מקיף 5/40 · 9/40 · 18/40.
**נזק ב-60 שנ' (`tests/m44-dash-dpm.js`, ממוצע 5; 31ג → 31ד):** עומד 1042 → 883 · הולך הלוך-חזור 466 → 334 ·
צעד-צעד לצדדים 472 → 493 · מקיף 292 → 271 · מסתתר מאחורי קיר 580 → 553 · צעד הצידה 786 → 689. העומד דווקא
**משלם פחות** — הרצף לוקח 3.7 שנ' שבהן האקדח שותק (348 במקום ~700). גיבוי: `backup-step31d/` (= 31ג+32 החי).
**הוחלט (2026-09-20): `dashWidth` נשאר 1.8.** מדדתי אותו מול רשת אמיתית, כי ההשהיה גורעת ~0.5 מ'
מההתרחקות שהשרת רואה: ב-1.8 צעד הצידה מציל 9 מ-12, ב-1.4 הוא מציל 12 מ-12. בחרת 1.8 — ההתחמקות
עובדת אבל לא בטוחה, ומי שמתמהמה חוטף. אם במשחק זה ירגיש עונש מדי — זה מספר אחד ב-`GHOST`
(וב-`BOSS_GHOST`), ו-`tests/m44-dash-dodge.js` מראה מיד מה זה עושה.

הבא: אחרי 31ד הרוח גמורה.
**32 (מקסימום החיים גדל עם כל בוס — בקשה שלך; `server.js` + ה-HTML) — ✅ עלה ואומת בשרת החי (2026-09-19):** commit
`3435ae6`, כל 11 הקבצים ב-Render זהים בבייטים, `tests/live-step32.js` 6/6 (בוס 1 הופל → `max-health` 125, ריפוי עד 125,
מצטרף מאוחר מקבל 125). כל בוס שהחדר
מפיל (חוץ מהדרקון, שמסיים את המשחק) מעלה את המקסימום של **כל מי שבחדר** ב-25 (100 → 125 → 150...) וממלא את ה-25 —
מי שעומד מקבל 25 מעל מה שהיה לו (70 → 95); מי שנפל נשאר על 0 ומקבל את המקסימום החדש כשהוא קם (בחייאה עדיין 50);
ריפוי (עצמי, הריגה, משימה) נעצר במקסימום החדש; מי שמצטרף באמצע נכנס עם המקסימום של החדר ומלא; משחק חדש / חדר שהתרוקן
→ 100. בשרת: `room.maxHealth` + `maxHealthOf(p)` + `raiseRoomHealth(room)`; `MAX_HEALTH` (100) עבר לראש הקובץ כי
`resetRoomState` רץ כבר בטעינה. בלקוח: `playerMaxHealth` + `setPlayerMaxHealth` (פס החיים = אחוז מהמקסימום, המספר =
החיים עצמם, שורה בפיד "MAX HEALTH +25 → 125"). אופליין — נשאר 100. נבדק: `tests/t45-maxhealth.js` 15/15 + כל החבילה;
בדפדפן — 125 מלא אחרי בוס, 75 = 60% בפס. גיבוי: `backup-step32/` (= 31א החי). אירוע חדש `max-health` — סעיף 9.
**33ב (🔥 EMBER RAIN — הדרקון יורק גחלים לשמיים; `boss.js` + `server.js` + ה-HTML) — נכתב ונבדק מקומית
(2026-09-23), עוד לא עלה.** מסמך התכנון: "דרקון – תכנון 3 מתקפות מיוחדות" (Claude Docs). כל 16 שנ' (הראשון 8 שנ'
אחרי שהגיע; בשלב 2 כל 11.2 — 30% פחות, כמו במסמך) כשיש מישהו חי עד 35 מ' — בלי צורך בקו ראייה: **ממריא** 6 מ'
(800ms, `TakeOff`) → **יורק** (917ms, `SpitUp`; 583ms פנימה הגחלים יוצאות) — ברגע הזה השרת בוחר את כל הנקודות:
**12 גחלים ועוד 2 לכל שחקן נוסף, עד 20**; חצי מהן סביב השחקנים — **הראשונה של כל שחקן בדיוק איפה שהוא עומד**, השאר עד
5 מ' ממנו — והחצי השני בכל מקום פתוח עד 16 מ' מהדרקון. כל אחת נוחתת 1200-1600ms אחרי שהטבעת נפתחה → **25** לכל מי שעד
1.6 מ' ממנה, ושלולית אש קטנה (1.5 מ', 3 שנ', 3 כל 0.6 שנ' = 5 לשנייה) → **מרחף** (`Hover`) עד שהאחרונה נשפטה → **נוחת**
(700ms). **נשפט כמו העיגול של הרוח** (250ms אחרי הנחיתה, לפי כל הדגימות 50-250ms אחריה; קפיצה לא עוזרת — זה מלמעלה).
בזמן הזה: לא זז, לא נושף, לא צולל (הצלילה מחכה 2 שנ' אחרי הנחיתה). **אפשר לירות בו כל הזמן** — למעלה באוויר: השרת
מגביה את בדיקת קו האש לפי `liftOf` (`server.js` `boss-hit`), אחרת קיר היה "מסתיר" דרקון שמרחף מעליו. נפל באוויר →
הגחלים שעוד באוויר נעלמות איתו, והוא נופל לקרקע ב-~0.8 שנ' ו-Death מתנגן. **המספרים שלא היו במסמך — שלי:** כל כמה זמן
(16 / 11.2), טווח 35, רדיוס פגיעה 1.6, 5 מ' סביב שחקן, 16 מ' סביב הדרקון, 1200-1600 (המסמך: 1.2), רדיוס השלולית 1.5, ה-2
לשחקן נוסף. **שלב 2 של הדרקון נשאר ב-30%** כמו לכל הבוסים (המסמך אומר 50% — שאלה ל-33ד). אופליין אין — הדרקון של משחק
יחיד נשאר עם הנשיפה והצלילה. בשרת: `DRAGON.ember*` (+ `p2.emberEveryMs`) = `BOSS_DRAGON` בלקוח (`t9` בודק), `b.ember`,
`tickEmber` / `throwEmbers` / `landEmbers` / `liftOf` (מיוצא), `sink.embers` → אירוע `boss-ember`, קודים 21-24 ב-snapshot;
ענן יכול לנשוך בקצב משלו (`c.every`). בלקוח: `EMBER` + `emberStep` / `updateEmber` / `emberFromSnapshot` / `emberClear`,
גובה הגוף = `emberLift` (אותה עקומה), טבעת כתומה פועמת + טבעת פנימית שנסגרת עד הרגע, הגחלת נופלת מ-40 מ' ב-0.9 השנייה
האחרונה (לא עוברת tone mapping — אחרת היא בז'), ניצוצות מהפה, צליל כנפיים / נשיפה / פגיעה קטנה, "⚠ MOVE ⚠" למי שעומד
בטבעת, שורות בפיד; `spawnFirePool(..., small)` לבום קטן יותר. **נבדק:** `tests/t46-ember.js` 39/39 ×3 (כולל: הולך בלי
להסתכל — זו שעליו לא פוגעת אף פעם, 1 פגיעה מהאחרות ב-3 מטחים; יוצא 60/130ms אחרי הנחיתה = ניצל, 400ms = נפגע);
`t37-dragon` מכבה את הגשם (`emberFirstMs = Infinity`) כדי לבדוק נשיפה וצלילה לבד — נכשלות בו לפעמים "keeps its distance" /
"swoops" / "two fireballs" גם על הקוד הישן (מיקום אקראי); `t19-scores` נכשל 2 (PvP) **גם על הקוד הישן**; שאר החבילה ירוקה.
`tests/live-step33b.js` מול שרת מקומי 9/9 (`PORT=3336 node run33.js` + `URL=http://localhost:3336 DRAGON_FIRST=1`: טבעת
0.00 מ' מתחתינו, נוחתת 1428/1471ms, עומד = −28 (25 + נשיכה), יוצא = 0/2, יריה כשהוא מרחף נספרת 2400 → 2280, החדר נוצח).
בדפדפן (`tests/run33.js` דרך `srv/run33b-page.js`, צד 3408, רתמה `tests/h33b.js`): כל 10 הקליפים, הרצף, הגובה 0 → 6 → 0,
הטבעות, הגחלות, השלוליות, ה-HUD, נפילה מהאוויר + ניצחון, מצב אופליין — בלי שגיאות; **+24 draw calls בממוצע בזמן הגשם
(מקסימום +65) על ~1,850**. גיבוי: `backup-step33b/` (= 33א החי).
**✅ עלה ואומת בשרת החי (2026-09-23):** commit `1fcbec6`, 4 הקבצים זהים בייט-בייט ב-GitHub וב-Render, ו-`tests/live-step33b.js`
מול Render 9/9 (בוסים 1-9 נפלו; טבעת 0.00/0.01 מ' מתחתינו, נוחתת 1286/1451ms; עומד = הגחלת שעליו פוגעת (1 ו-2 פגיעות);
יוצא = 0/2; יריה כשהוא מרחף 2400 → 2280; החדר נוצח; ~17.5 דק'). ירידת החיים בחלון נמדדה גבוהה (111-283) כי בשרת החי
הבנדיטים יורים ויש גם נשיפות — הפגיעות של הגחלים עצמן נספרות מאירועי `hit`.
**33ג (🐉 TAIL SWEEP — הזנב הסוחף; `boss.js` + `server.js` + ה-HTML) — נכתב ונבדק מקומית (2026-09-23), עוד לא עלה.**
הדרקון שומר מרחק, וזה מה שהוא עושה למי שלא: כשמישהו חי עד **7 מ'** ממנו (הראשון 3 שנ' אחרי שהגיע, אחר כך 7 שנ' אחרי שהקודם
נגמר; בשלב 2 4.9 — 30% פחות), והוא לא באוויר ולא בצלילה: **מתכופף** 790ms (`TailSweep` פריימים 0-19, נהמה — האזהרה, לא
מסתובב) → **מסתובב סיבוב שלם** ב-500ms (פריימים 19-31; `dir` אקראי) עם הזנב עד **8 מ'** → **קם** 625ms. כל שחקן נשפט **ברגע שהזנב
עובר אותו** (לפי הזווית שלו מהדרקון בתחילת הסיבוב), כמו העיגול של הרוח: דגימה אחת 50-250ms אחרי עם רגליים מעל 30 ס"מ (קפיצה) או
מעבר ל-8 מ' = ניצל; אחרת **40** וזריקה החוצה ב-13 מ'/ש' (הדף של הנפגע מזיז אותו — `TAIL_THROW` ב-`updatePlayer`, דועך תוך
חצי שנייה, קירות עוצרים, ~2 מ'). בזמן הזה: לא זז, לא נושף. צלילה וגשם מחכים 1.5 שנ' אחריו, והוא 1.5 שנ' אחריהם (אחרי צלילה —
מתחילתה + 1.4 + 1.5). **קפיצה בתחילת הסיבוב עוברת את הזנב כמעט בכל נקודה בו** (קפיצה מחזיקה את הרגליים מעל 30 ס"מ ~0.47 שנ'; בבדיקה:
400ms לפני שהזנב מגיע — עבר, 560 לפני — מוקדם מדי, 40 אחרי — עבר, 450 אחרי — מאוחר), ולכן ה-HUD אומר "⚠ GET OUT ⚠" בזמן
ההתכופפות ו-"⬆ JUMP ⬆" רק כשהסיבוב מתחיל — קפיצה בהתכופפות מוקדמת מדי. **המספרים שלא היו במסמך — שלי:** טריגר 7 מ', כל 7
שנ' (4.9 בשלב 2), 1.5 שנ' הפרדה, עוצמת הזריקה 13. בשרת: `DRAGON.tail*` (+ `p2.tailEveryMs`) = `BOSS_DRAGON`, `b.tail`,
`tailDue` / `startTail` / `tickTail` / `judgeTail` / `tailCrossMs`, `sink.tails` → `boss-tail`, קודים 25-27 ב-snapshot (שדה 11 =
`dir`). בלקוח: `TAIL` + `tailStep` / `updateTail` / `tailFromSnapshot` / `tailClear`, הסיבוב = `tailSpin` על ה-yaw (ליניארי — בדיוק
כמו השעון של השרת), עיגול אדום ברדיוס 8 עם טבעת שנסגרת עד הסיבוב, גל אבק שמתרחב בסיבוב ואבק מקצה הזנב (`tail5`), נהמה / רחש.
**נבדק:** `tests/t47-tail.js` 27/27 ×3 (כולל: 5 שחקנים סביבו נפגעים לפי הסדר שהזנב עובר אותם, כל אחד ±60ms; 7.5 מ' — לא מתחיל;
8.6 מ' — לא נפגע; יוצא מ-6 מ' בהליכה מההתכופפות — יוצא בזמן; זנב / צלילה / גשם אף פעם לא יחד; נפל באמצע — אין פגיעה; שוכב
downed — לא נפגע); `t46` ו-`t37` מכבים את הזנב (`tailFirstMs = Infinity`); `tests/live-step33c.js` מול שרת מקומי 8/8 (עומד = נפגע
×2 עם k 13; **קופץ כשהסיבוב על המסך = 0/3**; יוצא בהליכה = 0/2; החדר נוצח); בדפדפן (פורט 3337, צד 3408): הקליפ, העיגול, הסיבוב,
ה-HUD, **נזרק 5.7 → 7.6 מ'**, **קפיצה בתחילת הסיבוב דרך הדף עצמו = 0/3 פגיעות**, נפל באמצע הסיבוב (Death, העיגול נעלם), בלי
שגיאות. גיבוי: `backup-step33c/` (= 33ב החי).
**✅ עלה ואומת בשרת החי (2026-09-23):** commit `b33b76c`, 4 הקבצים זהים ב-GitHub (ושלושת קבצי המשחק גם ב-Render), ו-`tests/live-step33c.js`
מול Render 8/8 (בוסים 1-9 נפלו; התכופפות 802/811ms; עומד = נפגע ×2 ונזרק ב-13; **קופץ כשהסיבוב על המסך = 0/3 מעל רשת אמיתית**;
יוצא בהליכה = 0/2; החדר נוצח; ~17 דק').
**33ד1 (🔊 השאגה — שלב 2 בלבד; `boss.js` + `server.js` + ה-HTML) — נכתב ונבדק מקומית (2026-09-23), עלה ואומת (ראה בסוף).** 33ד חולק
לשניים (כמו הרוח): 33ד1 השאגה, 33ד2 הביצים והצאצאים. **הוחלט איתך:** שלב 2 של הדרקון נשאר ב-30% (לא 50% כמו במסמך), וה"טשטוש"
הוא טשטוש של כל המסך. רק בשלב 2: 3 שנ' אחרי המעבר, ואז 15 שנ' אחרי שהקודמת נגמרה, כשיש מישהו חי עד 45 מ': **החזה מתנפח**
1250ms (`Roar` פריימים 0-30, הגרון זוהר, נשימה עמוקה — האזהרה) → **השאגה** (פריים 30; 958ms עד סוף הקליף): **כל מי שעד 45 מ'
עם קו פתוח על הקרקע** (`nav.losClear` — כל קולידר חוסם, גם חבית, כמו במסמך) **מטושטש ורועד 2 שנ'**. בלי נזק. בזמן הזה: לא זז,
לא נושף; צלילה / גשם / זנב מחכים 1.5 שנ' אחריה והיא אחריהם. **המספרים שלא היו במסמך — שלי:** כל 15 שנ', 3 שנ' אחרי המעבר, טווח 45.
בשרת: `DRAGON.roar*` = `BOSS_DRAGON`, `b.roar`, `b.roarAt` (Infinity עד שלב 2, `enterPhase2` מכוון), `roarDue` / `startRoar` /
`tickRoar`, `sink.roarAttacks` → `boss-roar-attack` (**לא** `roars` — זה צליל הצלילה הישן), קודים 28-29. בלקוח: `ROAR` + `roarStep` /
`updateRoar` / `roarFromSnapshot` / `roarClear`; זוהר בגרון (ספרייט 0.7 מ' לפני `neck1` — בתוך הצוואר הגוף מסתיר אותו), 3 טבעות קול
שיוצאות מהפה, `sfxBossRoar` כפול, "⚠ TAKE COVER ⚠" בזמן ההתנפחות רק למי שבקו (אותה בדיקה כמו השרת, `roarLosClear`),
**טשטוש = `filter: blur()` של CSS על הקנבס בלבד** (ה-HUD נשאר חד), 7px שדועך תוך 2 שנ', ורעד. **לא נמדד על ה-UHD 620:** טשטוש CSS
על קנבס במסך מלא — אם זה מוריד פריימים בזמן השאגה, זה מספר אחד להוריד. **נבדק:** `tests/t48-roar.js` 17/17 (בשלב 1 אף פעם; 3 שנ'
אחרי המעבר; מאחורי קיר — מוגן; 46 מ' — לא בפנים בכלל; אף פעם לא יחד עם זנב / גשם / צלילה, 1.5 שנ' הפרדה; נפל בהתנפחות — אין
שאגה); `t37` / `t46` / `t47` מכבים את השאגה (`roarFirstMs = Infinity`); `tests/live-step33d1.js` מול שרת מקומי 7/7 (יורה עד שלב 2
ומפסיק; בחוץ = מטושטש ×2; מאחורי קיר = מוגן ×2; החדר נוצח); בדפדפן (פורט 3338): הקליפ, הזוהר, הטבעות, ה-HUD, הטשטוש נדלק ונעלם,
בלי שגיאות. גיבוי: `backup-step33d/` (= 33ג החי).
**✅ עלה ואומת בשרת החי (2026-09-23):** commit `bf4750f`, 4 הקבצים זהים ב-GitHub וב-Render, ו-`tests/live-step33d1.js` מול Render
7/7 (בוסים 1-9 נפלו; שלב 2 בלי שאגה לפניו; התנפחות 1247/1225ms; בחוץ = מטושטש ×2 ל-2000ms; קודים 28/29 בסנאפשוט; מאחורי קיר =
מוגן ×2; החדר נוצח; ~17 דק').
**33ד2 (🥚 הביצים והצאצאים — `bandits.js` + `boss.js` + `server.js` + ה-HTML) — נכתב ונבדק מקומית (2026-09-23), עלה ואומת (ראה בסוף).**
אחרי השאגה (אחרי שהיא נגמרת): **LayEggs** 500ms (קוד סנאפשוט 30), ו-250ms לתוכו הביצים נופלות בקשת לפניו, 3-4.5 מ' (ליד קיר — מסביב
עד 6 מ'). **הוחלט איתך:** 3 ביצים + 1 לכל שחקן נוסף בחדר, עד 5; תקרה 8 ביצים+צאצאים חיים (בתקרה — השאגה בלי הטלה); צאצא רץ
**6 מ'/שנ'** (מהר מהליכה 5.4, לאט מריצה 9.2); נשיכה **8 כל 1.2 שנ'**. מהמסמך: ביצה 15 חיים, בוקעת אחרי 3 שנ' (נורתה — מתפוצצת
בלי צאצא); צאצא 30 חיים, קופץ ב-2 מ'. **שלי:** הנשיכה נוחתת 250ms אחרי הקפיצה אם השחקן עדיין עד 2.6 מ' (נסיגה ברגע הקפיצה = התחמקות).
**איך:** הביצים והצאצאים הם רשומות ב-`room.bandits` (`kind` "egg" / "hatch", `summoned`) — אותו `bandit-hit`, אותו `bandit-died`,
אותו סנאפשוט עם **שדה 7** (1 ביצה + עשיריות שנייה לבקיעה, 2 צאצא; שודד רגיל נשאר 6 שדות). `BROOD` ב-`bandits.js` = `BOSS_BROOD`
ב-HTML (t9 בודק). הנשיכה = `sink.hits` כמו כדור, והקפיצה = אירוע `bandit-bite` (שורה אחת ב-`server.js`, `sink.bites`).
**התנועה של הצאצא** (`stepHatchling`) — לא כמו שודד, כי הוא צריך להגיע עד אליך: הרשת של השודדים מרפדת קירות ב-1.3 מ' ובתאים
גדולים, ומי שנצמד לקיר / בכלאה / במכרה לא נגיש בה. אז: ישר אליך כשהדרך פתוחה; עד 14 מ' — **רשת עדינה** של 0.5 מ' מול הקולידרים
עצמם (`FINE`, נבנית כולה בעליית השרת, ~130ms, כמו רשת ההליכה — בנייה תוך כדי עלתה 130ms בטיק); רחוק יותר — הרשת הרגילה, אל נקודת
גישה בצד שלך של הקיר (`approachPoint`), ובקצב של שודד (600-900ms — אחרת קפיצות של `findPath`). נמדד: מגיע ונושך 120/120 מוסתר
30-50 מ', 80/80 צמוד לקיר (רק מקומות ששחקן ברדיוס 0.42 באמת מגיע אליהם); עלות 8 צאצאים ~0.3-0.7ms לטיק (כמו 8 שודדים: 0.2-0.5).
**בלקוח:** ביצה = אליפסה אדומה מנומרת (טקסטורת קנבס), נופלת מהבטן בקשת, רועדת וזוהרת יותר ככל שהבקיעה קרבה; נורתה — מתפוצצת; בקעה —
קליפה מתעופפת. צאצא = `createBandit` עם `hatchType()` (הדרקון, `BOSS_LOOKS.hatchling` scale 0.55 = רבע מהבוס, `hoverK` 0.25 —
הריחוף 0.6 מ' של הדרקון היה מרים אותו לגובה החזה), Fly לריצה, Attack + קפיצה קטנה לנשיכה, Death; פס חיים לפי 30. בפיד: "Egg smashed" /
"Hatchling down". **בדפדפן (פורט 3339):** ההטלה, הביצים, הבקיעה, הצאצאים, ירייה בביצה (נשברה, לא בקעה), ירייה בצאצאים, תקרה 8 —
בלי שגיאות. **FPS על ה-UHD 620** (החלונית, מוסתרת ואיטית — הערכה): 8 צאצאים בפריים = +400 אלף משולשים, +24 draw calls, פריים
~85 → ~92ms (~8%). **נבדק:** `tests/t49-brood.js` 39/39 (כולל ליד קיר, תקרה, כמה ביצים לפי שחקנים, רדיפה); t48 מכבה את הביצים
(`BROOD.cap = 0`); החבילה כולה ירוקה (t46 נופל לפעמים על "no open spot" — גם בקוד של 33ד1, לא קשור); `tests/live-step33d2.js`
מול שרת מקומי 14/14 ×2. גיבוי: `backup-step33d2/` (= 33ד1 החי).
**✅ עלה ואומת בשרת החי (2026-09-23):** commit `6c46ad8`, 5 הקבצים זהים ב-GitHub וב-Render, ו-`tests/live-step33d2.js` מול
Render 14/14 (בוסים 1-9 נפלו; הטלה 1009ms אחרי הצעקה, 3 ביצים 249ms לתוכה; ביצה שנורתה נשברה ולא בקעה; השאר בקעו אחרי 3064ms;
מוסתר 49 מ' מאחורי מחסה — הגיעו ונשכו, כל 8 בא אחרי נשיכה; הופלו ב-bandit-hit; החדר נוצח; ~17 דק'). **הדרקון (שלב 33) גמור.**

**שדרוג המפה (M0–M13) — התוכנית ב-`PLAN-MAP.md` (לא להעלאה).** הוחלט איתך (2026-09-24): ביצועים קודם, הסגנון המצויר-חם של
היום משופר בקוד (בלי הורדות), וב-M1 הדברים שזזים (להבי הטחנה, גלגולי-מדבר, עגלת המשימה) מקבלים צל עגול. **כל שלב = ה-HTML בלבד;**
שום קובץ שרת ו-`map-data.js` לא משתנים. **החוק:** המפה נבנית ממחולל אקראי קבוע, ו-r128 צורך 4 מספרים אקראיים לכל אובייקט Three
חדש — אובייקט אחד נוסף בתוך הבנייה מזיז ~825 מ-909 הקירות (והשרת כבר לא באותה עיירה). לכן כל תוספת נבנית ב-`buildVisualLayer()`,
אחרי `Math.random = _systemRandom`, עם `makeSeededRandom(VISUAL_SEED + שלב)`; קישוטים בקבוצה `decor` (מחוץ ל-`world` — כדורים לא
נעצרים עליהם); לא מסתירים ולא מזיזים שום דבר שיש לו קיר.
**M0 (תשתית) — נכתב ונבדק מקומית (2026-09-24), עלה ואומת (ראה בסוף).** ב-HTML: הקטע `THE VISUAL LAYER` אחרי `mapFingerprint` (`VISUAL_SEED`,
`decor`, `buildVisualLayer()` ריקה); `?fps` מציג גם draw calls ומשולשים של פריים שלם (העולם + הנשק + הצללים — `renderer.info.autoReset`
כבוי רק עם `?fps`, ומאופס פעם בפריים). **שער לכל שלב במפה:** `node tests/map-audit/replay/sim.js` — בונה מחדש את העיירה מה-HTML
**החי** (בין סמנים קבועים) מול `map-data.js` החי, ומסיים ב-`GATE: PASS` (94b46e8e-909, 0 זזו) או `GATE: FAIL` + קוד יציאה 1; נכשל
גם אם `buildVisualLayer()` רצה לפני שהחלון נסגר. נבדק שהוא תופס: פנס אחד נוסף בתוך הבנייה (`node sim.js mesh+1`) = 826 קירות זזו,
FAIL. בדפדפן: offline (שרת סטטי) ו-online (שרת מקומי) — `mapFingerprint()` = 94b46e8e-909, `decor` בסצנה ולא ב-`world`, בלי שגיאות
(חוץ מ-socket.io.js בשרת הסטטי, צפוי); `?fps` = ~1,450 draws לפריים ברחוב (הבסיס ל-M1). t9, t7 19/19, t8 10/10. גיבוי: `backup-stepM0/`.
**✅ עלה ואומת (2026-09-24):** commit `9d33501` (ה-HTML + `HANDOFF.md`), זהים ב-GitHub וב-Render; קבצי השרת לא השתנו. הדף
שב-Render, מוגש מקומית ומחובר ל-Render (החלונית חסומה לאתר עצמו): `mapFingerprint()` = 94b46e8e-909, `decor` מחוץ ל-`world`, `?fps`
= 1,449 draws, בלי שגיאות. **M0 גמור. הבא: M1 (הקפאת הצללים).**
**M1 (צללי השמש נצבעים פעם אחת) — נכתב ונבדק מקומית (2026-09-24), עלה ואומת (ראה בסוף).** עד עכשיו קופסת הצל של השמש (124×124 מ') עקבה
אחרי השחקן וכל הצללים שבה נצבעו מחדש בכל פריים. עכשיו `bakeSunShadows()` (בשכבה הגרפית): השמש קבועה מעל מרכז העיירה באותו כיוון
בדיוק שהלולאה החזיקה (`SUN_BAKE.dir` = -60, 58, -66), קופסת הצל מותאמת לכל העיירה + הגבעות + המכרה (`SUN_BAKE.box`), מפה 4096 (2048
אם הכרטיס לא יכול) = 5.7/3.6 ס"מ לפיקסל צל (היה 6), `renderer.shadowMap.autoUpdate = false`, וצביעה אחת בזמן הטעינה — לפני שיש
דמות אחת בעולם. בלולאה: שלוש שורות "השמש עוקבת אחרי השחקן" ירדו. **מה שזז לא מטיל צל אמיתי** (היה נשאר מצויר במקום): גלגולי-מדבר
— צל עגול ב-`decor` שעוקב אחריהם (`visualUpdaters`, נקרא מ-animate); להבי הטחנה — בלי צל; עגלת המשימה (`makeWagon`) וריג הקוביות
(`createBoxRig`) — צל עגול משלהם (`newBlobShadow`: חומר וטקסטורה משלו לכל אחד, כי הם משחררים את שלהם כשמפנים אותם); שלטים
מתנדנדים (±4°) נשארו בצל הקבוע. `tests/map-audit/before/audit-helpers.js` לא מזיז את השמש כש-`SUN_BAKE` קיים. **נמדד** (החלונית,
UHD 620, M0 ↔ M1 לסירוגין באותו חלון, מ"ש לפריים): רחוב 40.5 → 26.4 (−35%), מלמעלה 39.0 → 29.9 (−23%), בפנים 23.0 → 13.0 (−43%);
`?fps` בנקודת ההתחלה: 1,449 → 299 draws לפריים. **צילומים** (25 הנקודות של הסריקה): זהה ברחוב, בבניינים ובמכרה, בלי פסים ובלי צל
מנותק; מלמעלה — עכשיו יש צל גם בחלקים הרחוקים (לפני כן רק ב-124 מ' סביב השחקן). שער: GATE: PASS. Offline, `?cubes` ו-online (שרת
מקומי, דרקון + 4 שודדים, 305 draws) — בלי שגיאות. t9, t7 19/19, t8 10/10. גיבוי: `backup-stepM1/` (= M0 החי).
**✅ עלה ואומת (2026-09-24):** commit `7349b89` (ה-HTML + `HANDOFF.md`), זהים ב-GitHub וב-Render; קבצי השרת לא השתנו. הדף
שב-Render, מחובר ל-Render: `mapFingerprint()` = 94b46e8e-909, `SUN_BAKE` קיים, `autoUpdate` כבוי, מפת צל 4096 קיימת, 7 צללים עגולים
לגלגולי-המדבר, `?fps` = 299 draws, בלי שגיאות. **M1 גמור. הבא: M2 (איחוד המפה לציור).**
**M2 (העיירה נצבעת בכמה חתיכות) — נכתב ונבדק מקומית (2026-09-24), עוד לא עלה.** `mergeTownForDrawing()` בשכבה הגרפית: כל mesh
ב-`world` שלא זז (לא תחת `animatedProps` — הטחנה והשלטים המתנדנדים נשארו כמו שהם) מועתק, כבר במקומו בעולם, לתוך mesh אחד לכל מראה
(חומר עם אותן הגדרות — `townMaterialKey` — + מטיל/מקבל צל + renderOrder): 1,237 → **56**. ההעתקים ב-`decor` (`town-drawn`, כדורים
עוברים דרכם); **המקוריים נשארו ב-`world`, מוסתרים** — ה-raycaster של r128 לא בודק `visible`, אז יריות, חורי כדורים וכל מה ששואל את
העיירה איפה היא — מקבלים בדיוק את אותן תשובות. חומרים שהם העתקים זהים (לכל קקטוס היה משלו) נצבעים כאחד — **לכן כל שינוי לחומר
או לצורה של העיירה בשלבים הבאים חייב לרוץ לפני המיזוג** (ב-`buildVisualLayer`: שלבי המראה → `mergeTownForDrawing` → `bakeSunShadows`
אחרון). **נמדד** (החלונית, M1 ↔ M2 לסירוגין, מ"ש לפריים): רחוב 28.3 → 14.9 (−47%), מלמעלה 44.7 → 16.2 (−64%), בפנים 11.0 → 10.2;
`?fps` בנקודת ההתחלה: 299 → **70** draws. **יחד עם M1, מ-M0:** רחוב 40.5 → 14.9 (פי 2.7), 1,449 → 70 draws. **צילומים:** 25 הנקודות
מול M1 — הפרש ממוצע 0.0-0.3 (0.85/1.4 בשתי נקודות: גלגול-מדבר שנכנס לתמונה ושלט מתנדנד — דברים שזזים). **יריות:** חור כדור נוחת
על הקיר (1.5 ס"מ ממנו, כמו תמיד), פגיעה בשודד עם קו פתוח נרשמת, מאחורי קיר — לא. Online (שרת מקומי, דרקון + 4 שודדים, 101 draws)
בלי שגיאות. שער: GATE: PASS. t9, t7 19/19 (פעם אחת 18/19 — ה"standing next to it" הידוע, t7 לא טוען את הדף), t8 10/10.
גיבוי: `backup-stepM2/` (= M1 החי).
**✅ עלה ואומת (2026-09-24):** commit `d731c0a` (ה-HTML + `HANDOFF.md`), זהים ב-GitHub וב-Render; קבצי השרת לא השתנו. הדף
שב-Render, מחובר ל-Render: `mapFingerprint()` = 94b46e8e-909, 56 חתיכות ב-`town-drawn`, 1,237 המקוריים מוסתרים וקרן עדיין פוגעת
בהם (ב-`decor` — 0), `autoUpdate` כבוי, 73 draws לפריים, בלי שגיאות. שער: GATE: PASS. **M2 גמור. הבא: M3.**
**M3 (סלעים שלמים, שלט מכרה אחד) — נכתב ונבדק מקומית (2026-09-24), עוד לא עלה.** `mendRocks()` בשכבה הגרפית, לפני המיזוג. הבאג:
`rockProp` מותח כל פינה של הדודקהדרון במידה אקראית משלה, אבל ב-r128 הדודקהדרון הוא "מרק משולשים" (כל פינה חוזרת פעם לכל משולש
שנוגע בה) — העותקים של אותה פינה זזו לכיוונים שונים והסלע התפרק לרסיסים. התיקון: כל פינה מקבלת את המתיחה שקיבל העותק הראשון שלה
(נקרא בחזרה מול דודקהדרון חדש באותו רדיוס — אותן פינות באותו סדר). אותו סלע, אותו גודל ומקום, סגור. 59 סלעים, לכל אחד בדיוק 20
פינות; אף אחד לא מרחף (שוקעים 6-64 ס"מ); הקירות שלהם נבנים מהרדיוס בלבד ולא השתנו; יריות פוגעות בצורה המתוקנת. **המכרה:** היו שני
שלטים — של `createEnterableBuilding` (ברוחב החזית, כמו בכל בניין) ו-`mineSign` קטן שנתלה מעליו; הקטן הוסר (אין לו קיר). 56 → 55
חתיכות ציור. שער: GATE: PASS, t9 עובר. צילומים: `m3_*` מול `tests/map-audit/before/` (10, 14c, 14d). גיבוי: `backup-stepM3/` (= M2 החי).
**M3 — גם המכרה (לבקשת המשתמש, באותה העלאה):** `dressMine()` בשכבה הגרפית, לפני המיזוג. היה: חדר קופסאות עם טקסטורת "לבנים"
מתוחה פי 3.6, רצפת חול בהירה, ועשרה אוקטהדרונים שטוחים שמרחפים באוויר (חלקם באמצע הפתח בין הבניין למנהרה). עכשיו: טקסטורת סלע
של מערה (`makeCaveRock`, סדרה משלה) על `MAT.rockDark` — מונחת לפי מטרים (`worldUV`, 3.5 מ' לאריח) על כל הקירות של המכרה, החזית
והמנהרה; רצפת עפר (`makeMineDirt`) מעל החול; תקרת סלע וקורות מתחת לגג הבניין; 4 מסגרות עץ במנהרה; סלע בולט מבסיס הקירות ומפינות
התקרה (לכל היותר ~0.3 מ' מהקיר); 11 אשכולות קריסטל שצומחים מהסלע (הגדול — ה"וריד" בקיר האחורי); עורקי זהב (גושים בקבוצות) בקירות;
מסילה על אדנים עם בולם ועגלה מלאה זהב; 4 מכושים (תקוע בעורק, שניים נשענים על קיר, אחד על הרצפה); כלובי מתכת סביב פנסי הקיר ושני
פנסים תלויים בבניין (במקום של `mineLight`/`mineDeepLight`). האוקטהדרונים הוסרו (אין להם קיר). אורות: טווח ארבעת פנסי המנהרה 7 → 10 מ'
(עמדו באמצע ולא הגיעו לקירות — הזהב נראה מרחף על שחור), הזוהר הכחול של החדר 1.0/10 → 1.5/13; הקוביות הזוהרות צבע להבה (0xffa64a).
**הכול קישוט:** כדורים ושחקנים עוברים דרכו, אין קירות חדשים — שני דברים על הרצפה (העגלה, מכוש) צמודים לקירות. יריות במכרה נוחתות על
הקירות כמו קודם (נבדק: הווריד, העורק השמאלי, העגלה). 11 חתיכות ציור, 7.4 אלף משולשים, בלי צל שמש (מתחת לגג); **מוסתר כשהמצלמה
יותר מ-60 מ' מהפתח** (`visualUpdaters`) — ברחוב 92 draws כמו לפני. `mergeTownForDrawing` פוצל: `mergeMeshesByLook(list)` הוא עכשיו
הכללי (אותה תוצאה לעיירה: 54 חתיכות אחרי ההסרות). צילומים: `tests/map-audit/m3/`. שער: GATE: PASS, t9 עובר, CRLF נשמר.

---

## 2א. שלבים 20-24 — מה נבנה, ואיך

**להעלאה של 20-24 (כולם יחד):** `map-data.js`, `navigation.js`, `bandits.js`,
`boss.js`, `server.js`, `wild-west-fps-arsenal.html`, `HANDOFF.md`, ו**קובץ חדש**
`missions.js`. לא השתנו: `waves.js`, `package.json`. **לא להעלות:**
`backup-step23/`, `tests/`. `map-data.js` חייב לעלות יחד עם ה-HTML — טביעת
האצבע של המפה השתנתה (`94b46e8e-909`).

**אם מעלים בשני סבבים:** קודם 20-23 = כל הקבצים מתוך `backup-step23/` (בלי
`missions.js`); אחרי שזה עובד בשרת החי — 24 = `missions.js` (חדש), `bandits.js`,
`boss.js`, `server.js`, `wild-west-fps-arsenal.html`, `HANDOFF.md` מהתיקייה הראשית.
`server.js` ו-`bandits.js` של 24 דורשים את `missions.js` — בלי הקובץ השרת לא עולה.

### 20 — סלעים
96 סלעים מסובבים בטבעת ברדיוס 72-76; באלכסונים הם בתוך הריבוע המשחקי (±68)
ולא היו להם תיבות — אפשר היה ללכת בתוכם וירי עבר דרכם. כל סלע נחתך לרצועות
של 1.25 מ' לאורך z (`mesaColliders()` בלקוח), רק החלק שבתוך הגבול. 134 →
909 תיבות. כדי שזה לא יאט: **אינדקס מרחבי** — תאים של 8 מ', בלקוח (`CGRID`)
ובשרת (`CG` ב-`navigation.js`), אותן תשובות בדיוק. נמדד: 0 אי-התאמות ב-100,000
שאילתות מול סריקה מלאה, רשת הניווט זהה בלקוח ובשרת (hash `18e64af1`), 99.98%
מנקודות הסלע חסומות, 3.96% מהקרקע ליד הטבעת נחסמת בטעות (עד רצועה אחת). השרת
**מהיר יותר** מלפני (0.70 → 0.53ms). נוצרו כיסים סגורים קטנים בין סלעים לקיר —
`randomNavPoint()` בשני הצדדים בוחר עכשיו רק מאזור העיר (+ המכרה, בכוונה).
הייצוא נעשה מהדפדפן לקובץ דרך שרת קבלה מקומי, בדיוק מלא.

### 21 — שלב 2 לבוסים
מתחת ל-30% חיים, פעם אחת: אש ×0.75 זמן, מהירות ×1.15, זוהר אדום, פס פועם,
ובנוסף לכל אחד: burst 5 כדורים (הוסר בשלב 30א — בוס 1 הוא השלד) · charge כל 2.8 שנ' · snipe מזמינה 3 בנדיטים
ומתרחקת ל-18 מ' · spray טבעת 12 כדורים כל 3 שנ' · dynamite 3 מקלות במניפה ·
ghost שתי הסתערויות Ghost Dash ברצף ו-Spectral Shot כל 5 שנ' (שלב 31ד; אופליין — נעלם כל 2.2 שנ') ·
slam רדיוס 9 כל 3.2 שנ' · poison 2 בקבוקים וענן 8 שנ'.
`PHASE2` ב-`boss.js`, `BOSS_PHASE2` ב-HTML (אותם מספרים, נבדק אוטומטית).
בנדיטים מוזמנים (`summoned`) לא קמים מחדש ולא נספרים בתקרת הגל.

### 22 — חיבור מחדש
שחקן שהתנתק הוא **away** ל-20 שניות: שומר חדר, slot, ניקוד, חיים ומיקום;
הסימולציה לא רואה אותו (`activePlayers()`), והגוף מוסתר אצל האחרים. הדפדפן
שומר אסימון ב-`sessionStorage` ושולח ב-`auth`; גם רענון דף חוזר לאותו מקום.
**המזהה של שחקן = ה-socket הראשון שלו**; handlers מוצאים אותו דרך
`socket.data.pid`. **רשומות שחקן לא נשלחות יותר כמו שהן** — `publicPlayer()`
— אחרת האסימון היה דולף וכל אחד יכול לגנוב מקום. אסימון של שחקן מחובר (לשונית
כפולה) = שחקן חדש. אירועים חדשים: `player-away`, `player-back`.

### 23 — החייאה
**רק במצב שיתופי.** חיים 0 → `downed` (לא מת, לא חוזר לבד). חבר עומד בטווח
2.5 מ' ומחזיק E → `revive-start`; השרת סופר 5 שניות, מאמת טווח (עד 3.5 מ')
וחיים; עזיבת E / התרחקות / ניתוק מבטלים. קם עם 50 חיים. **כלל שהוספתי כי
בלעדיו החדר נתקע:** אם אין אף שחקן מחובר עומד בחדר — כולם קמים אחרי 3 שניות;
אם מישהו נכנס בזמן הזה, הספירה מתבטלת (`team-wiped {cancelled:true}`).
FFA נשאר עם התחייה רגילה אחרי 4 שניות. **E ליד חבר שנפל = החייאה; בכל מקום
אחר E ממשיך לטעון.** שחקן שנפל לא יכול לזוז (השרת מתעלם מ-`move`). סמן אדום
מעל חבר שנפל (נראה דרך קירות) וצלב אדום במפה הקטנה.

### 24 — משימות (נכתב ונבדק מקומית)
**קובץ חדש `missions.js`** מחזיק את כל החוקים והמספרים (`MISSION`). `server.js`
מודיע לחדר ומשלם (`startMission`/`finishMission`); `bandits.js` מקבל את המטרה
דרך `missions.targetFor()` ומודד פגיעות בבנק ובעגלה (`sink.missionHits`).
מה שהשתנה מול התכנון למטה:
- העגלה מתחילה **מהקצה הקרוב לשחקנים** מבין שני המסלולים ושני הכיוונים, ולא
  חוזרת על אותו מסלול פעמיים ברצף. אזור השטח נבחר אקראית, לא אותו אחד פעמיים.
- בנדיט שנמשך למטרה נכנס ל-chase מיד (הוא יודע איפה הבנק), ועוצר כשהוא רואה את
  המטרה ובטווח `keep`: בנק 9, עגלה 3, שטח 3 (כלומר בתוך הטבעת). בשטח הוא לא יורה.
- **`mission-state` מקסימום 2.5 בשנייה, ורק כשמשהו שהדף לא יכול לחשב השתנה**
  (חיים, מיקום עגלה, מי חוסם/מחזיק); אחרת פעם בשנייה. השעון והתקדמות השטח רצים
  בדף בין חבילות. נמדד: ~115 B/s JSON לשחקן כשהעגלה נוסעת, ~32 B/s בבנק שקט.
  (גרסה ראשונה של 5 בשנייה = ~215 B/s — קוצץ בגלל תקציב ה-5GB.)
- עגלה ממשיכה לנסוע בדף עד 0.45 שנ' מעבר לחבילה האחרונה, לא יותר.
- מצב שחקן יחיד: **אין משימות**. נבדק שלא נשבר.

**נמדד (`tests/t32`, `t33`, `t34`):** 26 + 21 בדיקות, 3 ריצות רצופות כל אחת בלי
כישלון; כל חבילת הבדיקות הישנה (t7-t31) עוברת. עלות בשרת: אין הבדל מדיד (4 חדרים
× 16 בנדיטים: 0.38 בלי משימה → 0.26-0.35 עם, רעש). בנק בלי מגינים: שורד בגל 2,
נופל אחרי 31-38 שנ' בגל 4 ו-24-31 שנ' בגל 6. אחרי 25 שנ' 7 מתוך 8 בנדיטים זוגיים
ליד הבנק, 0 מתוך 8 אי-זוגיים. **עגלה בגל 6 עם מלווה שלא יורה בכלל: נהרסת אחרי
15-38 שנ'** — כי כדורים שמכוונים אל המלווה פוגעים בה (לפי התכנון). בדפדפן:
פאנל, עמוד אור בבנק, עגלה נוסעת, טבעת שמשנה צבע, כוכב במפה הקטנה, חזרה אחרי רענון.

**מלכודות שהתגלו:** (1) בבדיקה שמנקה בנדיטים — גם `room.nextSpawnAt = Infinity`,
אחרת תגבורת אחרי 15 שנ' עם id זוגי הולכת לעגלה וחוסמת אותה. (2) מעבר גל עושה
`fillTo` — לנקות בנדיטים *אחרי* שהמשימה התחילה. (3) חלונית הדפדפן מוסתרת = אין
`requestAnimationFrame` בכלל, גם בלשונית פעילה.

התכנון המקורי, לעיון:
- מתחילה משימה **בכל מעבר גל** (מגל 2), בסבב בנק → עגלה → שטח, עד 40 שנ'.
  בוס שמגיע מבטל את המשימה בלי עונש.
- **הגנה על הבנק:** 700 חיים. הבנק = collider 1 `[-15.75,0,-16.5,-6.25,4.4,-8.5]`.
  נקודת הדלת לניווט `(-11,-6.6)` (פנויה ונגישה, נבדק); יורים על מרכז הקופסה
  `(-11,1.8,-12.5)` כדי שכל כיוון יפגע בקיר. מצליחה אם הבנק עומד אחרי 40 שנ'.
- **ליווי עגלה:** 450 חיים, 2 מ'/ש', זזה רק כששחקן בטווח 8 מ' ואין בנדיט
  בטווח 4 מ'. מסלול: `z=0.5` מ-`x=-12` עד `x=45` פנוי לעגלה ברוחב 1.4
  (נבדק: 57.5 מ'), או `z=0` מ-`x=-32` עד `x=20` (53 מ'). ~50 מ' → 25 שנ' תנועה.
  כדור שעובר בטווח 1.4 מ' ממרכז העגלה (גובה 1) פוגע בה.
- **החזקת שטח:** טבעת 6 מ'. התקדמות כששחקן חי בפנים ואין בנדיט בפנים; 22 שנ'.
  מקומות שנבדקו פתוחים ונגישים: `(0,0)`, `(0,40)`, `(-25,45)`, `(-30,-30)`,
  `(30,30)`, `(40,-8)`. **לא** `(-38,0)` (חסום).
- **פרס:** +25 חיים לכל שחקן עומד (שרת), +30 תחמושת (לקוח). כישלון: בלי פרס.
- **בנדיטים:** חצי מהם (id זוגי, לא summoned) נמשכים למטרה, אלא אם שחקן בטווח
  10 מ'. התכנון הנקי: ב-`stepBandit` להחליף את `near` באובייקט מטרה מדומה
  `{id:'#mission', x, z, y, aim?, noFire?, keep?}` — כל שאר הקוד (LOS, ניווט,
  טווח, ירי, פנייה) עובד כמו מול שחקן. `fire()` יורה על `target.aim || target`.
  פגיעות: ב-`stepBullets` — כשהקליע נחסם וחוצה את קופסת הבנק → נזק לבנק;
  מרחק מקטע מהעגלה < 1.4 → נזק לעגלה.
- **פרוטוקול מתוכנן:** `mission` בהתחלה, `mission-state` 5 פעמים בשנייה,
  `mission-end {k, ok, why}`, ומצב נוכחי בתוך `room-joined`.
- **לקוח:** פאנל למעלה (שם + פס + שעון), בנק = עמוד אור בדלת, עגלה = מודל
  פשוט עם אינטרפולציה, שטח = טבעת על הקרקע בצבע לפי מצב; סימון במפה הקטנה.

---

## 3. הקבצים

| קובץ | תפקיד |
|---|---|
| `wild-west-fps-arsenal.html` | כל המשחק — ~236KB, קובץ יחיד |
| `server.js` | Express + Socket.IO, חדרים, שחקנים, אימות פגיעות, לולאת הסימולציה |
| `bandits.js` | סימולציית הבנדיטים בשרת: ספawn, ניווט, מרדף, ירי, קליעים |
| `boss.js` | הבוס בשרת: 10 טיפוסים (הרובוט מ-27, הדרקון האחרון מ-28), יכולות, טיימר, דינמיט, רעל ואש |
| `waves.js` | **חדש (13)** — הגל של החדר, וכמה בנדיטים הגל מרשה |
| `missions.js` | **חדש (24)** — בנק / עגלה / שטח: התחלה, צעד, סיום, ומי מהבנדיטים נמשך |
| `navigation.js` | התנגשויות + A* בשרת (תרגום מדויק של הלקוח), **ועכשיו גם** בדיקת קו אש, אזורי רשת ותקציב חיפוש |
| `map-data.js` | **קובץ מיוצר** — 134 תיבות התנגשות של המפה |
| `package.json` | express ו-socket.io, גרסאות נעולות |

**ארבע כפילויות מכוונות בין הלקוח לשרת. אם אחת מהן נשברת, המשחק נשבר בשקט:**

1. `map-data.js` מיוצא מלקוח רץ. אם המפה בלקוח משתנה — לייצא מחדש ולוודא
   ש-`FINGERPRINT` תואם ל-`mapFingerprint()` בלקוח.
2. `BOSS_TYPES` קיים ב-`boss.js` וב-HTML. השרת שולח **אינדקס** למערך, אז
   הסדר הוא חלק מהפרוטוקול. יש בדיקה אוטומטית שמשווה את שתי הטבלאות.
3. עקומת הקושי (`6 / +2 / 16 / 45 שניות`) קיימת ב-`waves.js` וב-HTML.
   אונליין השרת מנצח; העותק בלקוח משרת את מצב השחקן היחיד.
4. עקומת הבנדיט (שלב 19) — `DIFFICULTY` ב-`waves.js` ו-`BANDIT_DIFFICULTY`
   ב-HTML. אותו כלל: אונליין השרת מנצח, והלקוח משמש את מצב השחקן היחיד.
5. מקלע הרובוט (שלב 27) — `ROBOT` ב-`boss.js` ו-`BOSS_ROBOT` ב-HTML. `t9-tables`
   בודק גם אותם.
6. הדרקון (שלב 28) — `DRAGON` ב-`boss.js` ו-`BOSS_DRAGON` ב-HTML, וגם `radius` /
   `keepDistance` / `final` בשורות של `BOSS_TYPES`. `t9-tables` בודק את כולם, וגם
   שיש בוס `final` אחד בדיוק והוא האחרון ברשימה.
7. השלד (שלב 30) — `SKELETON` = `BOSS_SKELETON`; הרוח (שלבים 31ב-31ד) — `GHOST` = `BOSS_GHOST`.
   `t9-tables` בודק את שתיהן, וגם שאין בדף שם שמוגדר פעמיים ברמה העליונה.

---

## 4. הארכיטקטורה — מה רץ איפה

### השרת מחזיק
- מי בכל חדר, ומצב החדר (מוד, בנדיטים, בוס, **גל**)
- **חיים של שחקנים** ומי מת ומתי חוזר
- **טבלת הנזק של כל הנשקים** — הלקוח לעולם לא אומר כמה נזק
- כל סימולציית הבנדיטים: מיקום, AI, ירי, קליעים
- כל סימולציית הבוס: מי, מתי, תנועה, יכולת, קליעים, דינמיט, ענן רעל
- **הגל ועקומת הקושי** — כמה בנדיטים מותר שיעמדו בו-זמנית
- אימות: טווח, קצב אש, מספר כדוריות, חדר משותף

### הלקוח מחזיק
- כל הרינדור, הקול, ה-VFX
- תנועת השחקן עצמו
- **כל המשחק לשחקן יחיד** — כשאין שרת, הכל רץ מקומית כמו שהיה

### מודל האמון
הלקוח מדווח **שפגע ואיפה בגוף** — לא כמה. השרת דוחה: נשק לא חוקי, קצב
בלתי אפשרי, מספר כדוריות שהנשק לא יורה, מרחק מחוץ לטווח, פגיעה בעצמך,
פגיעה בין חדרים, ופגיעה שדווחה על ידי שחקן מת.

**קו ראייה נאכף מאז שלב 15.** כל דיווח פגיעה — בבנדיט, בבוס או בשחקן — נבדק:
האם חלק כלשהו מהגוף (ראש, חזה, אחת הכתפיים) נראה מעיני היורה, במיקום הנוכחי
של המטרה או בכל מקום שהייתה בו ב-300ms האחרונים. מה שעדיין **לא** נתפס: ירי
דרך פינת אחת המסות הגדולות בקצוות המפה, שאין להן תיבת התנגשות בכלל.

`health-delta` נשאר **להתאוששות ולבונוס הריגה בלבד** — עם תקרות
(מקסימום 20 לדיווח, 5 דיווחים בשנייה).

---

## 5. החלטות שחשוב להבין

**מפה דטרמיניסטית.** המפה נבנתה אקראית בכל טעינה — כל שחקן ראה סלעים
במקומות אחרים. הפתרון: `Math.random` מוחלף ב-PRNG עם זרע **רק לאורך בניית
המפה** (`MAP_SEED` בלקוח), ומוחזר מיד אחרי.

**דיוק מלא ב-map-data.js.** עיגול לשתי ספרות הפך 4 תאי ניווט ש*נוגעים*
בקיר לתאים ש*חופפים* לו. אחרי ייצוא בדיוק מלא: **0 אי-התאמות ב-14,112 תאים.**

**אינטרפולציה של 110ms.** גופים מרוחקים מצוירים מעט בעבר. `INTERP_DELAY`.

**קליעים נוסעים, לא פגיעה מיידית.** גם בשרת, כדי שאפשר יהיה להתחמק. כל
קליע נושא את המהירות והנזק שלו — צלף 72 מ'/ש' ו-32 נזק, בנדיט 42 ו-10.

**מצב שיתופי הוא ברירת המחדל.** באש ידידותית כבויה שחקנים **לא נמצאים
בנתיב הירי בכלל** — הכדור עובר דרך בן ברית ופוגע בבנדיט מאחוריו.

**8 בחדר, ובכל שלוש הדלתות.** המגבלה הייתה נאכפת רק בהקלדת קוד: הלובי היה
בלי מגבלה בכלל, וקישור שיתוף עקף אותה לגמרי. עכשיו הלובי הוא **כמה לובאים
קטנים** — נכנסים למלא ביותר שיש בו מקום, וכשכולם מלאים נפתח `PUBLIC2` וכן
הלאה עד 6. קישור לחדר מלא אומר למה ושם אותך בלובי במקום להיות התשיעי בחדר
של שמונה. מבקש לובי מלא מועבר בשקט ללובי עם מקום — הוא ביקש לשחק, לא ביקש
את החדר הזה בדיוק.

**חדר מתאפס כשהוא עובר מריק למאויש** — בנדיטים, בוס **וגל**. אחרת הנכנס
הבא יורש גל 9 ו-16 בנדיטים. מאז שלב 28 זה `resetRoomState()` ב-`server.js` — אותו
איפוס בדיוק משמש חדר חדש, חדר שמתמלא, ו"משחק חדש" אחרי ניצחון (והוא גם מנקה את `won`).

**התעבורה מקובצת, ומיקום נשלח כמספר שלם.** שלושת רבעי מה שהמשחק עלה היה
חבילות `player-moved` נפרדות. עכשיו זו חבילה אחת לחדר ב-15Hz, ממוענת ב-slot
קצר במקום מזהה סוקט, נושאת רק מי שזז, במספרים שלמים. נמדד: פי 3.5 פחות
תעבורה בחדר של 8, פי 7 בחדר של 16, והמעבד ירד מ-50% ליבה ל-11% ב-16.
**הרווח האמיתי הוא שהעלות בחדר כבר לא גדלה בריבוע** — מספר ההודעות לשחקן
זהה ב-2 שחקנים וב-16.

**קו האש נבדק רק מול תיבות גבוהות מ-2.21 מטר.** כל תיבת התנגשות במפה נמתחת
לגובה של לפחות 2.2 מטר (`box.max.y = Math.max(box.max.y, 2.2)` בלקוח) כדי שאי
אפשר יהיה לעלות על ארגז — כך שחבית של מטר נראית לשרת כמו קיר. נמדד מול קרן
הכדור האמיתית של הדפדפן ב-4,000 זוגות אקראיים: כל התיבות = 1.16% מהפגיעות
הכשרות נדחות; רק הגבוהות (46 מתוך 134) = 0.10%.

**ההיסטוריה של 300ms היא מה שמחזיק את זה.** הדפדפן מצייר גופים 110ms בעבר,
ודיווח פגיעה לוקח עוד הלוך-חזור. בלי היסטוריה: ~1% דחיות כשהמטרה זזה. איתה:
~0.13%. **נמדד מקצה לקצה** — לקוח אמיתי, raycast אמיתי, בנדיטים זזים, רשת:
107 פגיעות שהלקוח רשם, 107 שהשרת קיבל. השרת מדפיס כל 5 דקות כמה פגיעות נבדקו
וכמה נדחו, כדי שאפשר יהיה לראות את המספר גם במשחק אמיתי ב-Render.

**לוח התוצאות שייך לחדר.** הריגות, מיתות, נזק לבוס (מה שהירייה *לקחה*, לא מה
שהייתה שווה — הירייה האחרונה על בוס עם 20 נשארו נספרת 20) והריגות בוס.
נשלח רק כשמשהו משתנה, לכל היותר פעם בשנייה; חדר שקט לא שולח כלום. Tab מציג,
והוא קופץ לבד ל-5 שניות כשבוס נופל.

**הגל קובע גם כמה כל בנדיט מסוכן (שלב 19).** ארבעה מנופים, כל אחד בקו ישר
מגל 1 — שהוא המשחק בדיוק כפי שהיה — עד גל 6, שם גם התקרה נעצרת:

| | גל 1 | גל 6 |
|---|---|---|
| חזרה אחרי מוות | 2.6 שניות | 1.5 |
| זמן בין יריות | 1.75 שניות | 1.15 |
| פיזור הכוונת | ×1.0 | ×0.6 |
| חיים | 100 | 150 |

נמדד: שחקן בגל 6 חוטף פי 1.5 נזק לדקה כשהוא בתנועה (264 → 393) ופי 1.7 כשהוא
עומד, מעבר למה שהתקרה לבדה עושה; וצריך יותר יריות להפיל בנדיט (וינצ'סטר לגוף
3 → 5, סנייפר לגוף 1 → 2, ראש בסנייפר עדיין אחת). **בנדיטים שכבר עומדים
מתחזקים כשהגל עולה, ושומרים על הפצע** — פצוע 60/100 בגל 1 הוא 110/150 בגל 6.
כך לכל בנדיטי החדר אותו מקסימום, והלקוח מצייר את פס החיים מולו (`hp` בחבילת
הגל). הבוס לא מושפע — יש לו מספרים משלו.

**שלב 17 לא נכתב, כי הוא לא היה עושה כלום.** נמדד: טיימר התגבורת של 15 שניות
מוסיף 2 בנדיטים בגל 1, אחד בגל 2, **ואפס מגל 3 והלאה**. מגל 2 העיר נמצאת על
100% מהמכסה כל הזמן, גם כשהורגים בנדיט כל שתי שניות — כי בנדיט מת חוזר תוך
2.6 שניות, וכל מעבר גל ממלא את העיר מיד. הקצב האמיתי הוא ה-2.6 שניות האלה.

**A* היה כמעט כל עלות השרת, ורוב זה היה חיפושים שנכשלו.** בקושי המקסימלי,
כשהשחקנים זזים: 97% מזמן הסימולציה היה A*, ו-97% ממנו חיפושים שלא מצאו מסלול
— כל אחד מיצה 9,000 צעדים (~8ms) לפני שוויתר, וחזר על עצמו כל 600ms. הסיבה:
**הרשת לא בחתיכה אחת.** הריפוד של 1.3 מטר סביב כל קיר אוטם שני מקומות ששחקן
כן נכנס אליהם — המכלאה המגודרת צפונית לכיכר (31 משבצות), והמכרה (1,360
משבצות), שהכניסה שלו ברוחב 3.6 מטר נשארת עם פחות ממשבצת אחת. הפתרון: האזורים
מחושבים פעם אחת בעלייה, וחיפוש בין שני אזורים מכוון ל**נקודה הקרובה ביותר
שאפשר להגיע אליה** במקום לחפש עד מיצוי. בנדיט שרודף אחרי שחקן במכלאה מגיע
לגדר ויורה מעליה. 0.22ms במקום 8.

**ותקציב חיפוש לטיק, וחימום בעלייה.** 16,000 משבצות לטיק לכל החדרים יחד;
מעבר לזה `findPath` מחזיר `undefined` ("לא בטיק הזה", שונה מ-`null` = "אין
מסלול") והבנדיט ממשיך במסלול שיש לו. 0.4% מהחיפושים נדחים בטיק. והטיקים
הראשונים של שרת טרי היו איטיים גם עם התקציב כי V8 עוד לא הידר את A* — 400
חיפושים לפני שהפורט נפתח (~0.6 שניות) הורידו את הטיק הגרוע מ-148 ל-48ms.

**הגל הוא חוגת הקושי.** גל 1 מרשה 6 בנדיטים, וכל גל מוסיף 2 עד תקרה של 16.
כשגל מתחלף העיר מתמלאת **מיד** לחוזק החדש — לא טפטוף של אחד כל 15 שניות —
כדי שהמעבר יורגש. 45 שניות לגל, בדיוק כמו בדפדפן.

**16 הוא מספר מדוד, לא מספר עגול.** חדר עם 16 בנדיטים ובוס עולה ~2ms מתוך
טיק של 50ms, והטיקים האיטיים נוחתים סביב 17ms. ב-20 הטיקים האיטיים עוברים
25ms והגרועים חורגים מהתקציב לגמרי — וזה מורגש אצל **כל** השחקנים בכל
החדרים כבנדיטים שמחליקים על הקרקע. אם יום אחד יהיה שרת חזק יותר,
`maxCap` ב-`waves.js` הוא המספר היחיד לשנות.

**גל קופא בזמן קרב בוס, וההריגה קונה גל.** כמו בדפדפן. המשמעות: **קבוצה
שלא מצליחה להרוג את הבוס לא מתקדמת** — אין גל חדש, אין בוס חדש, ובנדיטים
מתים לא קמים. זה המבנה של המשחק, לא תקלה.

**הרווח היחיד מהמקור: הטלפורט של הרוח — ומה שהחליף אותו.** בדפדפן
`GHOST-WALKER COLE` קפץ לנקודה אקראית על כל המפה — ב-80% מהמקרים 40 מטר משם,
מחוץ לטווח הירי שלו ומחוץ ליכולת של מישהו למצוא אותו. אונליין זה גרוע יותר:
בוס שאי אפשר להרוג עוצר את החדר לנצח. לכן הקפיצה הוגבלה ל-8 עד 26 מטר
(`blinkMin`/`blinkMax`). נמדד: 0 נזק לדקה לפני, ~600 אחרי.

**בשלב 31ד הקפיצה הזו ירדה מהשרת לגמרי** וה-Ghost Dash החליף אותה: היא תמיד
יוצאת 7 מטר ממישהו, אז הבעיה ש-`blinkMin`/`blinkMax` פתרו פשוט לא קיימת עוד.
**אופליין (שחקן יחיד) ה-VANISH נשאר** — המוח המקומי שלם ב-HTML ואינו עולה
לשרת כלום, ובלעדיו היה נשאר בוס 7 בלי שום יכולת במשחק היחיד.

---

## 6. מלכודות שהתגלו בדרך

- **לשונית ברקע מפסיקה לשדר.** הדפדפן מקפיא `requestAnimationFrame`. כדי
  לבדוק שני שחקנים לבד — **שני חלונות נפרדים, לא שתי לשוניות.**
- **קצב שידור = min(20Hz, fps).** אם ה-fps נמוך, גם השידור.
- **`Box3.intersectsBox` סופר נגיעה כחפיפה.** מכאן בעיית העיגול.
- **קליע מהיר מדלג מעל מטרה.** 2.1 מ' לטיק מול ספירה ברוחב 1.24 מ' = 37%
  מהפגיעות הישירות אבדו. הפתרון: בדיקת מרחק למקטע, לא לנקודה.
- **`/` מחזיר 404 בלי redirect.** יש redirect ב-`server.js`.
- **בדיקות headless חייבות שעון שמתחיל מ-`Date.now()`.** `bandits.js` חותם
  טיימרים מול השעון האמיתי. `boss.js` ו-`waves.js` מקבלים `now` כפרמטר.
- **`moved` בקוד הבוטים אומר "ניסה לזוז", לא "זז".** `moveAxis` מסרב בשקט
  כשחסום. בגלל זה שחרור-מתקיעות ישן בדק `moved && כמעט לא זז` — וזה היה
  נכון, אבל הפתרון שלו היה להגריל נקודה אקראית על כל המפה ולקחת אותה רק
  אם נפלה בטווח 6 מטר. מתוך בניין זו הגרלה של 1 ל-500, כלומר בוט שנתקע
  באמת נשאר תקוע לתמיד. עכשיו יש `nav.freeSpotNear()` שסורק טבעות על רשת
  הניווט ומוצא את המשבצת הקרובה שהגוף נכנס בה. נמדד: שחרור תוך 0.8-1.6
  שניות מאמצע הבניין הכי גדול במפה.

- **כשהדפדפן לא מצייר, raycast משקר.** `updateServerBandits` מזיז את
  `mesh.position`, אבל המטריצות שה-Raycaster משתמש בהן מתעדכנות רק ברינדור.
  כשחלון הבדיקה מאחורי חלון אחר, `requestAnimationFrame` קפוא והגופים היו
  **32 מטר** ממקום שהקרן חשבה. בבדיקות: `scene.updateMatrixWorld(true)` לפני ירי.
- **שרת קבצים סטטי + דפדפן = גרסה ישנה מהמטמון.** `python -m http.server` לא
  שולח כותרות מטמון, והדפדפן הריץ HTML של שלב קודם. בבדיקות: `?v=` בכתובת.
  (Express ב-Render שולח `max-age=0`, אז בייצור זה לא קורה.)
- **`BANDIT.fireDelay` ו-`BANDIT.maxHealth` כבר לא משפיעים.** מאז שלב 19 השרת
  קורא את העקומה (`waves.DIFFICULTY`), ו-`BANDIT` נשאר רק כנקודת ייחוס של גל 1.
  בדיקה שרוצה בנדיטים שלא יורים צריכה `DIFFICULTY.fireDelay = [1e9, 1e9]`.
- **כיוון כדור בחבילה מעוגל ל-3 ספרות, וזה משקר ל-acos.** רכיב קדימה של 0.9996
  שמתעגל ל-1.000 הופך החטאה של 27 מילי-רדיאן ל-21 כשמחשבים `acos` של המכפלה.
  המשחק לא נפגע (הלקוח מצייר לפי יחסי הרכיבים — 4 ס"מ ב-40 מטר), אבל בדיקה
  חייבת לנרמל לפני שהיא מודדת זווית.
- **זמני טיק מוחלטים תלויים בעומס על המחשב.** אותו מבחן נתן 3.58ms כששרתים
  אחרים רצו ברקע ו-0.70ms כשלא. להשוות תמיד לפני/אחרי באותה ריצה.
- **כלי ה-Bash מבטל backslash ב-heredoc.** `\\n` הופך לשורה חדשה אמיתית ו-`\\u2014`
  לתו — טלאים שמכילים `\\` חייבים להיכתב לקובץ עם Write ולהריץ משם.
- **סבב של כלי לוקח יותר מ-3 שניות.** אי אפשר לתזמן מבחוץ תרחיש שחלון הזמן
  שלו 3 שניות (כמו ביטול wipe) — בודקים אותו בסוקט, לא בדפדפן.
- **בדיקות שמשוות שתי ריצות אקראיות נותנות תוצאות הפכפכות.** לבדוק את מה
  שהמנגנון באמת משנה (מרווח בין יריות, מהירות מקסימלית) ולא ספירות.
- **"2 מטר מהבוס" נופל בתוך בניין ב-6.6% מהמקרים.** מאז שלב 15 זה אומר שהשרת
  דוחה את הירי — בצדק. בדיקות צריכות לבחור נקודה שאפשר לעמוד בה ושיש ממנה קו אש.

---

## 7. איך נבדק כל שלב

לא "נראה בסדר" — מדידה. הדפוס שחזר על עצמו:

1. שרת מקומי עם עותק של כל הקבצים
2. שני לקוחות בדפדפן, השוואת מצב ביניהם
3. הרצות headless ב-Node לסימולציות ארוכות ולמדידת ביצועים
4. ניסיונות תקיפה מפורשים על כל נתיב אימות בשרת
5. בדיקת רגרסיה שמצב אופליין לא נשבר — **בכל שלב**

**נמדד בשלב 12:** כל 8 הבוסים 60 שניות ×5 הרצות (כולם גורמים נזק, אף אחד
לא הולך דרך קיר); 10 דקות × 4 חדרים בלי דליפה; הבוס מוסיף 1.6ms לטיק;
19 בדיקות על סוקט אמיתי; 10 בדיקות הצטרפות באמצע קרב; השוואת טבלאות
אוטומטית. עלות רשת: ~690 בתים לשנייה לשחקן.

**נמדד בשלב 19:** כל מנוף נמדד מהסימולציה ולא נקרא מהטבלה — חיים מ-`hurt()`
אמיתי, חזרה ממוות אמיתי (2600/1500ms), קצב ירי מיריות שנורו בפועל (33.6 → 51
לדקה, ×1.52), דיוק מהכדורים עצמם (×0.58-0.62); 6 בדיקות סוקט עם שעון גלים מקוצר
(כל חבילת גל נושאת את חיי הגל; פצוע 63 → 113); בדפדפן אונליין (פס 113/150 =
0.708 בדיוק) ואופליין (אותם ערכים, פיזור ×0.60 על 400 יריות). ואימות בשרת החי של
15/16/18: 12 מתוך 12 טענות פגיעה מאחורי בניין נדחו, 8 מתוך 8 בשטח פתוח נקלטו,
8 שחקנים ב-9.9 תמונות מצב בשנייה. ואחרי העלאת 19, בשרת החי עם השעון האמיתי:
חדר חדש = גל 1 ו-100 חיים; בנדיט נפצע ל-63; אחרי 47 שניות גל 2 הגיע עם `hp: 110`,
הפצוע עלה ל-73 וכל השבעה האחרים ל-110; ו-8 שחקנים עדיין ב-10 תמונות מצב בשנייה.

**נמדד בשלבים 15-18:** קו אש — 4,000 זוגות מול קרן הדפדפן (0.10% דחיות
כשרות), היסטוריה מול מטרות זזות (1% → 0.13%), 9 בדיקות סוקט (דרך קיר נדחה,
קו פתוח נוחת, "הסתתר לפני רגע" נוחת), ו-107/107 מקצה לקצה. לוח תוצאות — 11
בדיקות שרת (כולל "הירייה האחרונה נספרת לפי מה שלקחה" ו"חדר שקט לא שולח"),
ובדפדפן אונליין ואופליין. A* — 3 ו-5 דקות של 4 חדרים בגל 6 עם שחקנים שרצים:
ממוצע 26.75 → 3.58ms, p99 245 → 14.7ms, גרוע 473 → 56ms, טיקים מעל התקציב
16% → 0.03%. ושתי בדיקות ישנות שהתגלו הפכפכות (מיקום בתוך בניין, מרוץ טיימר)
תוקנו כך שהחבילה אמינה לפעם הבאה.

**נמדד בשלב 13:** עלות טיק לפי תקרה (6/8/12/16/20/24) — משם נבחר 16;
18 בדיקות על לוגיקת הגלים (השעון, הקפאה בקרב בוס, הריגה שקונה גל, התקרה,
חדר ריק שלא מתקדם); 13 בדיקות על סוקט אמיתי עם השעון האמיתי של 45 שניות;
6 בדיקות שחרור-מתקיעות; ובדפדפן — מעבר גל חי (1→2, 5→8 בנדיטים מיידית,
+16 תחמושת) והריגת בוס (2→3, 8→10 בנדיטים, נשק נפתח).

---

## 8. מספרים למי שמכוון

ב-`waves.js`, אובייקט `WAVE`:

```
everyMs: 45000       a wave is 45 seconds
startCap: 6          bandits at once in wave 1
perWave: 2           and two more every wave
maxCap: 16           measured ceiling - see section 5
ammoReward: 16       rounds when a wave turns over
```

ב-`bandits.js`, אובייקט `BANDIT`: `startCount` (3), `spawnEveryMs` (15000),
`fireDelay` (1750), `bulletDamage` (10), `fireRange` (38), `respawnMs` (2600).
**אין יותר `maxAlive`** — התקרה מגיעה מהגל.

ב-`boss.js`, אובייקט `BOSS`: `firstBossMs`/`nextBossMs` (90000),
`keepDistance` (9), `radius` (0.85), `chargeSpeed`/`chargeDamage` (14, 22),
`slamRadius`/`slamDamage` (6.5, 24), דינמיט (38 נזק, 7.5 מ', דועך),
ענן רעל (6 נזק כל 0.45 שניות למשך 5.2 בתוך 4.2 מ'). `blinkMin`/`blinkMax` **ירדו בשלב 31ד**.
`BOSS_TYPES` — חיים, קצב אש ומהירות לעשרה (ולחלק גם `radius`, `keepDistance`, `final`).
`GUN` — כדורים, פיזור, נזק ומהירות לכל יכולת. `ROBOT` (27) ו-`DRAGON` (28) — המספרים של
שני הבוסים עם המודל: לדרקון מהירות הנשיפה (18), השלולית (4.2 מ' / 8 / 4 שנ'), העיטה
(22 מ' / 16 מ'/ש' / 30 / כל 6 שנ'), ושלב 2 (4 בנדיטים, 2 כדורים, עיטה כל 3.5 שנ').
`GHOST` (31ב) — ה-Spectral Shot: `spectralFirstMs`/`spectralEveryMs` (5000 / 7000; `p2` 5000), `spectralRange` (45),
`raiseMs`/`flashMs`/`lockMs`/`lowerMs` (250 / 600 / 250 / 500), `spectralDamage` (60), `spectralSpeed` (60),
`spectralReach` (50). **`lockMs` הוא הכפתור של הקושי:** ב-250 כל מי שזז מתחמק; קטן יותר = צריך לזוז מהר יותר.
`GHOST` (31ג) — ה-Grave Burst: `graveFirstMs`/`graveEveryMs` (10000 / 13000), `graveRange` (30), `graveRaiseMs`/
`graveFillMs`/`graveLowerMs` (500 / 1100 / 350), `graveRadius` (2.5), `graveDamage` (40), `gapMs` (2500 — בין סוף
Spectral לתחילת Grave ולהפך). הקפיצה נשפטת עם המספרים של הטבעת הנמוכה של השלד (`SKELETON.lowClear` /
`dodgeFromMs` / `dodgeToMs`) — לא מספרים משלה. **הכפתורים של הקושי:** `graveFillMs` (כמה זמן יש לצאת) ו-`graveRadius`
(כמה רחוק צריך ללכת); `tests/m43-grave-dpm.js` מקבל `RADIUS=` / `FILL_MS=` בלי לגעת בקוד.
`GHOST` (31ד) — ה-Ghost Dash: `dashFirstMs`/`dashEveryMs` (8000 / 12000), `dashRange` (40), `mistMs`/`dashGoneMs`/
`markLeadMs`/`appearMs` (500 / 1000 / 700 / 450), `appearDist` (7), `dashSpeed` (16), `dashReach` (12), `dashMaxMs` (750),
`dashWidth` (1.8), `dashDamage` (35), `recoverMs` (1000), ו-`p2` (`dashes` 2, `dashMistMs` 400, `dashGoneMs` 400).
**שני הכפתורים של הקושי:** `dashWidth` (כמה צריך להתרחק מהקו — מעל רשת אמיתית 1.8 משאיר רבע
מהצעדים הצידה נפגעים, 1.4 כמעט אף אחד) ו-`dashSpeed` (כמה זמן יש להגיב); `tests/m44-dash-dodge.js`
מקבל `WIDTH=` / `SPEEDS=` ו-`tests/m44-dash-dpm.js` מקבל `WIDTH=` / `DASH_SPEED=`, בלי לגעת בקוד.

ב-`server.js`: `RESPAWN_MS` (4000), `WEAPONS`, `SPAWNS` (12 נקודות
מאומתות), `ROOM_LIMIT` (8, נמדד — ראה סעיף 8א), `MAX_LOBBIES` (6), `MODES`.

ב-`server.js` גם: `MOVE_SEND_HZ` (15) — קצב חבילת המיקומים.

ב-`waves.js` גם `DIFFICULTY` — ארבעה זוגות `[גל 1, גל fullAtWave]`: `respawnMs`,
`fireDelay`, `spreadScale`, `health`, ו-`fullAtWave` (6). זה המקום לכוונן קושי.

ב-`missions.js`, אובייקט `MISSION`: `ms` (40000), `order`, `healthReward` (25),
`bank.hp` (700), `wagon.hp`/`speed`/`escortRange`/`blockRange` (450, 2, 8, 4),
`hold.radius`/`needMs` (6, 22000), `attractRange` (10), `sendEveryTicks` (8).
ה-30 תחמושת נמצאים בלקוח, ב-`onMissionEnd`.

ב-`navigation.js`: `SHOT_MIN_HEIGHT` (2.21) — מה עוצר כדור; `SHOT_SKIN` (0.06) —
כמה פינה נותנים ליורה; `PATH_BUDGET` (16000) — משבצות חיפוש לטיק לכל השרת.
ב-`bandits.js`: `TRAIL_LENGTH` (7 טיקים = 300ms) — כמה אחורה בודקים קו אש.

בלקוח: `INTERP_DELAY` (110), `NET_SEND_HZ` (20), `MAP_SEED`,
`WAVE_START_CAP`/`WAVE_CAP_STEP`/`WAVE_MAX_CAP`/`WAVE_MS`.

**עמידה במקום בשטח פתוח = מוות תוך ~10 שניות.** נמדד.
**עמידה במקום מול בוס = 600 עד 2300 נזק לדקה, תלוי מי הגיע.** גם נמדד.

---

## 8א. כמה אנשים זה מחזיק — נמדד על השרת החי

נמדד על השרת החי לפני שלב 14, ואז מקומית אחרי:

| בחדר | לכל שחקן (לפני → אחרי) | סה"כ יוצא | הודעות לשנייה | מעבד |
|---|---|---|---|---|
| 2 | 2.6 → **1.8** KB/ש | 5 → 4 | 29 → 25 | — |
| 4 | 5.9 → **2.4** KB/ש | 24 → 10 | 65 → 24 | — |
| 8 | 12.6 → **3.6** KB/ש | 101 → 29 | 137 → 25 | — |
| 16 | 25.6 → **3.7** KB/ש | 410 → 59 | 268 → 25 | 50% → **11%** מליבה |

ולפני השינוי, על השרת החי: 8 בחדר החזיקו 10.0 תמונות מצב בשנייה גם בגל 6 עם
16 בנדיטים; 24 אנשים בארבעה חדרים — 9.8; 16 באותו חדר — 9.6 והתחיל להתכופף.

**הדבר החשוב בטבלה הוא עמודת ההודעות.** היא שטוחה עכשיו. קודם כל תזוזה של כל
שחקן נשלחה בנפרד לכל שאר החדר, 20 פעמים בשנייה — גדילה בריבוע, 16 שחקנים =
4,300 הודעות בשנייה. עכשיו זו חבילה אחת לחדר, 15 בשנייה, בכל גודל. **חדר של
16 עולה היום פחות ממה שחדר של 8 עלה אתמול.**

**התעבורה היא המגבלה האמיתית, לא המעבד.** תוכנית Hobby ב-Render כוללת
**5GB תעבורה יוצאת לחודש**, מתאפסת בתחילת כל חודש קלנדרי, בלי גלגול של
יתרה — ובלי אמצעי תשלום מקושר, חריגה **מכבה את השירות עד תחילת החודש הבא**.
(מאז אוגוסט 2025 תשובות WebSocket נספרות, וזה כל מה שהמשחק הזה עושה.)

מה שזה אומר בשעות משחק בחודש:

| שכבה | לשעה | 5GB מספיקים ל- |
|---|---|---|
| 2 שחקנים | 22MB → **14MB** | 230 → **355 שעות** |
| 4 שחקנים | 86MB → **36MB** | 58 → **139 שעות** |
| 8 שחקנים | 363MB → **104MB** | 14 → **48 שעות** |
| 8 בגל 6 | 490MB → **144MB** | 10 → **35 שעות** |
| 16 בחדר | 1.4GB → **212MB** | 3.5 → **24 שעות** |

(שתי העמודות הראשונות בטבלה הזו: 2 שחקנים ~14MB לשעה = ~355 שעות,
4 שחקנים ~36MB לשעה = ~139 שעות.)

---

## 9. הפרוטוקול (מה עובר על החוט)

מהשרת ללקוח — הבוס:

| אירוע | מתי | תוכן |
|---|---|---|
| `boss-spawn` | פעם אחת, בכניסה | `{t, hp, x, z}` — `t` אינדקס ב-`BOSS_TYPES` |
| `boss` | 10/שנייה כשהוא חי, 1/שנייה כשלא | `{t, hp, b:[x,z,yaw,health,ghost,charge,phase2,gatling]}` או `{in: שניות}` — `gatling` (שלב 27): 1 מסתובב, 2 יורה, 0 אחרת |
| `boss-shot` | לכל קליע | `{k, o, d}` — `k: "robot"` נורה מאמצע הרובוט לכיוון שהוא פונה; הלקוח מצייר מהקנה אל הקו הזה |
| `boss-boom` `k: "steam"` | סוף כל צרור של הרובוט בשלב 2 | `{i, k, p, l, r}` — ענן קיטור: רדיוס 3, 4 נזק כל 0.45 שנ', 3 שנ' |
| `boss-slam` `k: "stomp"` | שחקן בטווח 4 מ' מהרובוט, פעם ב-4 שנ' | `{p, k, r}` — רדיוס 5, נזק 20 |
| `boss-hazard` / `boss-boom` | דינמיט ורעל | `{i, k, o, v, f}` / `{i, k, p}` |
| `boss-slam` / `boss-roar` | יכולות | `{p, k}` |
| `boss-blink` | **לא נשלח יותר** — ה-VANISH ירד מהשרת בשלב 31ד (GHOST DASH במקומו) | `{p}` |
| `boss-died` | מוות | `{by, t, x, z, headshot, nextIn}` |

מהשרת ללקוח — הגל:

| אירוע | מתי | תוכן |
|---|---|---|
| `wave` | פעם בשנייה, ומיד כשמתחלף | `{n, cap, hp, in, up?, why?}` — `hp` = חיי בנדיט בגל הזה |
| `room-joined` | בכניסה לחדר | נושא גם `wave`, `cap` ו-`banditHp` |

`up: true` הוא הרגע עצמו — הוא מביא את שורת הפיד ואת התחמושת. השאר הוא
דופק שמחזיק מצטרף חדש על אותו מספר.

מהשרת ללקוח — לוח התוצאות (שלב 16):

| אירוע | מתי | תוכן |
|---|---|---|
| `scores` | רק כשמשהו השתנה, לכל היותר פעם בשנייה | `{s: [[slot, kills, deaths, bossDamage, bossKills], ...]}` |
| `room-joined` | בכניסה | נושא גם `scores` |

מהשרת ללקוח — מקסימום החיים (שלב 32):

| אירוע | מתי | תוכן |
|---|---|---|
| `max-health` | כל בוס שהחדר מפיל (חוץ מהאחרון) | `{max, add}` — `max` המקסימום החדש של כולם בחדר (100 → 125 → 150...), `add` = 25; החיים עצמם באים כרגיל ב-`player-health` |
| `room-joined` / `room-restarted` | בכניסה / משחק חדש | נושאים גם `maxHealth` (שרת ישן בלי השדה = 100) |

מהשרת ללקוח — משימות (שלב 24):

| אירוע | מתי | תוכן |
|---|---|---|
| `mission` | מעבר גל, מגל 2 | `{k, ms, x, z}` + בנק/עגלה `hp, h` + עגלה `e:[sx,sz,ex,ez]` + שטח `r, need, p` |
| `mission-state` | עד 2.5/שנ' כשמשהו השתנה, אחרת 1/שנ' | `{l: עשיריות שנ' שנשארו, c: חסום, g: מתקדם, h?, x?, z? (ס"מ), p? (עשיריות שנ')}` |
| `mission-end` | סיום | `{k, ok, why}` — `survived`/`arrived`/`held`, `destroyed`/`time`, `boss`/`wave` |
| `room-joined` | בכניסה | נושא גם `mission` (אותו מבנה כמו `mission`, או `null`) |

מהשרת ללקוח — איפה כולם (שלב 14):

| אירוע | מתי | תוכן |
|---|---|---|
| `players` | 15 בשנייה לחדר | `{m: [[slot, x, y, z, yaw, pitch], ...]}` |

**הכל מספרים שלמים** — מיקום בסנטימטרים, זוויות במאיות רדיאן. לא בשביל דיוק
(סנטימטר ושליש מעלה הם הרבה מתחת למה שרואים על גוף מאונטרפל) אלא כי
`-1234` קצר על החוט מ-`-12.34`, והחבילה הזו היא רוב מה שהמשחק עולה.
אותו דבר בחבילת הבנדיטים. **החבילה נושאת רק מי שזז** — עמידה במקום לא עולה
כלום. גם השורה שלך בפנים, ואתה מדלג עליה; לשלוח חבילה אחת לחדר זה סריאליזציה
אחת, ולהוציא את עצמך היה אומר לבנות חבילה שונה לכל מאזין.

**ה-slot** הוא מספר קטן שמשמעותו "השחקן הזה" בתוך החדר הזה. מחולק בכניסה,
מוחזר ביציאה, ומגיע ללקוח ב-`room-joined` וב-`player-joined`. בלעדיו כל שורה
הייתה נושאת מזהה סוקט של 20 תווים, 15 פעמים בשנייה, לכל שחקן.

מהלקוח לשרת: `move` ב-20Hz (נכנס, לא נספר במכסה), `boss-hit` עם
`{w, body, head}`, `bandit-hit` עם `{w, targets:[{id, body, head}]}`.
כל השאר נקבע בשרת.

טעינה נראית והחייאה בכניסה (שלב 26ג):

| אירוע | כיוון | מתי | תוכן |
|---|---|---|---|
| `reload` | לקוח → שרת | תחילת טעינה | `{ms}` — אורך הטעינה, 300-12000. נדחה: לא מספר, מת, נפל, away, פחות מ-500ms מהקודם |
| `player-reload` | שרת → כל החדר חוץ מהשולח | ממסר של `reload` | `{id, ms}` — מראה בלבד, השרת לא סופר תחמושת. 61 בתים; ~28 B/s לשחקן בחדר של 8 שטוענים כל 15 שנ' |
| `room-joined` / `player-joined` / `player-back` | שרת → לקוח | בכניסה | שורה של שחקן שנפל ומישהו כבר מחייה אותו נושאת `revive: {by, ms, left}` (אחרת אין שדה) — מצטרף באמצע רואה את המחייה כורע ואת הפס ממשיך |

הדרקון וסוף המשחק (שלב 28):

| אירוע | כיוון | מתי | תוכן |
|---|---|---|---|
| `boss-hazard` `k: "fire"` | שרת → חדר | נשיפה (כל 1.6 שנ', בשלב 2 שניים במניפה) | `{i, k, o, v, f}` — יוצא ממרכז הדרקון בגובה 2.83 ונוחת על הקרקע ב-`o + v·f` בדיוק אחרי `f` שנ' (כבידה 18). השרת מזיז אותו לפי השעון, לא צעד-צעד. הלקוח מצייר מהפה לאותה נקודה באותו רגע |
| `boss-boom` `k: "fire"` | שרת → חדר | הנחיתה | `{i, k, p, l: 4, r: 4.2}` — שלולית אש: 8 נזק כל 0.45 שנ', הראשון באותו טיק |
| `boss-slam` `k: "swoop"` | שרת → חדר | עיטה שהגיעה | `{p, k}` — 30 נזק למי שנפגע. תחילת העיטה = `boss-roar`, ודגל ה-charge ב-`boss` |
| `boss-died` | שרת → חדר | הבוס האחרון נפל | כמו תמיד, ועוד `won: true`, `nextIn: 0` — אין פרס נשק ואין גל; `room-won` מגיע מיד אחריו |
| `room-won` | שרת → חדר | הדרקון נפל | `{by, t, x, z, headshot, wave, ms, scores: [[slot, kills, deaths, bossDamage, bossKills, id], ...]}` — `ms` = משך המשחק. מכאן החדר קפוא: אין `boss`/`wave`, `bandits` פעם בשנייה (כולם מתים), אין פגיעות FFA |
| `room-joined` | שרת → לקוח | כניסה / חזרה לחדר | נושא גם `won` (אותו אובייקט, או `null`) — מצטרף לחדר שניצח רואה את מסך הניצחון |
| `room-restart` | לקוח → שרת | "משחק חדש" | בלי תוכן. רק בחדר שניצח, פעם בשנייה לשחקן; הלחיצה הראשונה מאפסת והשאר נופלות על חדר שכבר לא ניצח |
| `room-restarted` | שרת → חדר | האיפוס | `{by, wave: 1, cap, banditHp, scores}` — ואחריו `player-respawn` לכל אחד בחדר (100 חיים, נקודת התחייה) |

דו-קרב בצהריים של השלד (שלב 30ב):

| אירוע | כיוון | מתי | תוכן |
|---|---|---|---|
| `boss-duel` `e: "mark"` | שרת → חדר | כל 15 שנ' (הראשון 6 שנ' אחרי שהגיע), על שחקן אקראי שהוא רואה עד 32 מ' | `{e, p, ms: 3000}` — `p` מזהה השחקן המסומן. השלד עומד במקום ולא יורה עד הסוף |
| `boss-duel` `e: "shot"` | שרת → חדר | הספירה נגמרה | `{e, p, hit, o, at}` — `hit` = קו ראייה (`nav.losClear`) ועד 45 מ'; אם כן, 80 נזק דרך `player-health` הרגיל. `o` מאיפה, `at` איפה השחקן עמד |
| `boss-duel` `e: "break"` | שרת → חדר | `boss-hit` עם `head > 0` מכל שחקן בזמן הספירה | `{e, by}` — אין ירייה; השלד על ברך 2 שנ' (לא זז, לא יורה) |
| `boss-duel` `e: "lost"` | שרת → חדר | המסומן נפל / מת / עזב באמצע | `{e, p}` |
| `boss` | שרת → חדר | כמו תמיד | **רק בזמן דו-קרב** נוספים 3 שדות: `[..., 1, id המסומן, ms שנשארו]` או `[..., 2, "", ms של הברך]`; אחרת 8 שדות כמו קודם (לקוח ישן מתעלם) |

ההתפרקות של השלד (שלב 30ג) — פעם אחת, כשהוא יורד לחצי:

| אירוע | כיוון | מתי | תוכן |
|---|---|---|---|
| `boss-scatter` `e: "collapse"` | שרת → חדר | הטיק שאחרי שירד ל-450 | `{e, p: [x, z], ms: 1000}` — Collapse. דו-קרב שרץ מתבטל (בלי `lost`) |
| `boss-scatter` `e: "gone"` | שרת → חדר | +1000ms | `{e, p, tgt, ms: 2000}` — אין גוף ואין פגיעות. `tgt` = השחקן שאליו הוא בא (אקראי מכל מי שבחיים; `""` אם אין) |
| `boss-scatter` `e: "rise"` | שרת → חדר | +2000ms | `{e, p, y, tgt, ms: 1250}` — המקום המדויק ו-yaw; `tgt: ""` = קם איפה שנפל (המטרה נפלה/עזבה) |
| `boss-scatter` `e: "strike"` / `"blow"` / `"done"` | שרת → חדר | +1250 / +375 / +458ms | בין `strike` ל-`blow` הוא מזנק אל המטרה (16 מ'/ש', עד 1.2 מ' ממנה) — מגיע ללקוח דרך המיקום ב-`boss`. `blow`: `{e, p, hit: [ids]}` — `p` איפה שהזינוק נגמר; 30 דרך `player-health` הרגיל. `done`: חוזר לקרב |
| `boss` | שרת → חדר | כמו תמיד | בזמן ההתפרקות: `[..., 3/4/5/6, tgt, ms שנשארו]` (collapse / gone / rise / strike) |

קציר העצמות של השלד (שלב 30ד):

| אירוע | כיוון | מתי | תוכן |
|---|---|---|---|
| `boss-harvest` `e: "summon"` | שרת → חדר | 14 שנ' אחרי שהגיע, אחר כך כל 22 (3 שנ' לפחות מדו-קרב) | `{e, ms: 1500}` |
| `boss-harvest` `e: "spin"` | שרת → חדר | +1500, +1000, +1000 | `{e, i: 0-2, k: "low"/"high", ms: 1000}` |
| `boss-harvest` `e: "ring"` | שרת → חדר | 300ms לתוך כל סיבוב | `{e, i, k, p: [x, z], v: 9, r0: 1, max: 24}` — הרדיוס = `r0 + v·t` |
| `boss-harvest` `e: "cut"` | שרת → חדר | 250ms אחרי שהטבעת הגיעה לשחקן | `{e, i, k, p: id, hit}` — `hit` = 25 דרך `player-health` הרגיל |
| `boss-harvest` `e: "tired"` / `"done"` | שרת → חדר | אחרי הסיבוב השלישי / +3000 | `tired: {e, ms: 3000}` |
| `boss` | שרת → חדר | כמו תמיד | בזמן קציר: `[..., 7/8/9, "low"/"high"/"", ms שנשארו]` (summon / spin / tired) |

ה-Spectral Shot של הרוח (שלב 31ב) — קודי ה-snapshot של הרוח הם 11-19:

| אירוע | כיוון | מתי | תוכן |
|---|---|---|---|
| `boss-spectral` `e: "aim"` | שרת → חדר | 5 שנ' אחרי שהגיעה, אחר כך כל 7 (שלב 2: 5) | `{e, p: id המטרה, ms: 850, fl: 600}` — `ms` עד שהקליע יוצא, `fl` כמה מזה הבזק (ה-250 הראשונות = הרמת האקדח). נשלח שוב (עם ה-`ms` שנשאר) אם המטרה נפלה והיא עברה לאחר |
| `boss-shot` `k: "spectral"` | שרת → חדר | +850ms | `{k, o: [x, y, z], d: [dx, dy, dz], r: 50}` — הכיוון ננעל 250ms קודם. **הלקוח לא עוצר אותו בקירות**; 60 דרך `player-health` הרגיל |
| `boss-spectral` `e: "done"` | שרת → חדר | +500ms (האקדח ירד) | `{e}`; `{e, off: 1}` = בוטל בלי ירייה (אין אף אחד בטווח) |
| `boss` | שרת → חדר | כמו תמיד | מה-`aim` ועד הירייה: `[..., 11, id המטרה, ms עד הירייה]` |

ה-Grave Burst של הרוח (שלב 31ג):

| אירוע | כיוון | מתי | תוכן |
|---|---|---|---|
| `boss-grave` `e: "raise"` | שרת → חדר | 10 שנ' אחרי שהגיעה, אחר כך כל 13 (2.5 שנ' לפחות מ-Spectral) | `{e, i, p: id המטרה, ms: 500}` — `ms` עד שהעיגול נפתח |
| `boss-grave` `e: "cast"` | שרת → חדר | +500ms | `{e, i, p: [x, z], r: 2.5, ms: 1100}` — איפה שהמטרה עמדה ברגע הזה, `ms` עד הפריצה |
| `boss-grave` `e: "burst"` | שרת → חדר | +1100ms | `{e, i, p: [x, z]}` — הידיים יוצאות (הלקוח מצייר) |
| `boss-grave` `e: "judge"` | שרת → חדר | +250ms אחרי הפריצה | `{e, i, hit: [ids], safe: [ids]}` — מי שהיה בעיגול בפריצה: `hit` ספג 40 (דרך `player-health` הרגיל), `safe` יצא או קפץ |
| `boss-grave` `e: "done"` | שרת → חדר | +350ms אחרי הפריצה (היד ירדה) | `{e}`; `{e, off: 1}` = בוטל לפני העיגול (אין אף אחד בטווח) |
| `boss` | שרת → חדר | כמו תמיד | היד עולה: `[..., 12, id המטרה, ms עד העיגול]`; העיגול מתמלא: `[..., 13, id המטרה, ms עד הפריצה, x, z]` (13 שדות) |

ה-Ghost Dash של הרוח (שלב 31ד) — **החליף את `boss-blink`**:

| אירוע | כיוון | מתי | תוכן |
|---|---|---|---|
| `boss-dash` `e: "mist"` | שרת → חדר | 8 שנ' אחרי שהגיעה, אחר כך כל 12 (2.5 שנ' לפחות מ-Spectral ומ-Grave) | `{e, p: [x, z], tgt: id המטרה, ms: 500}` — איפה היא מתפוגגת. בשלב 2 השנייה באה עם `ms: 400, again: 1` |
| `boss-dash` `e: "gone"` | שרת → חדר | +500ms | `{e, p: [x, z], tgt, ms: 1000}` — **אין גוף**: `boss-hit` מחזיר `null`, והלקוח מוציא אותה מרשימת המטרות ומהמפה הקטנה. בשלב 2 השנייה היא 400 |
| `boss-dash` `e: "mark"` | שרת → חדר | 700ms לפני שהיא יוצאת | `{e, p: [x, z], tgt, ms}` — מערבולת הערפל על הקרקע, בדיוק במקום שבו תצא. `tgt: ""` = לא נמצא מקום (המטרה נפלה, או היא בפתח צר מדי) |
| `boss-dash` `e: "appear"` | שרת → חדר | +1000ms מה-`gone` | `{e, p: [x, z], y: yaw, tgt, ms: 450}` — היא **מועברת לשם** (לא הלכה), והלקוח מנקה את חוצץ האינטרפולציה |
| `boss-dash` `e: "dash"` | שרת → חדר | +450ms | `{e, o: [x, z], d: [dx, dz], ms: 750}` — הכיוון ננעל בדיוק עכשיו, על איפה שהמטרה עומדת ברגע הזה |
| `boss-dash` `e: "hit"` | שרת → חדר | בזמן ההסתערות | `{e, ids: [ids], p: [x, z]}` — מי שהיא עברה דרכו: 35 דרך `player-health` הרגיל, פעם אחת לשחקן בהסתערות |
| `boss-dash` `e: "recover"` | שרת → חדר | כשההסתערות נגמרה (בשלב 2 — רק אחרי השנייה) | `{e, p: [x, z], ms: 1000}` — עומדת ולא יורהת |
| `boss-dash` `e: "done"` | שרת → חדר | +1000ms | `{e}`; `{e, off: 1}` = בוטל בלי הסתערות (המטרה נפלה / עזבה, או לא נמצא מקום לצאת בו) |
| `boss` | שרת → חדר | כמו תמיד | `[..., 14/15/16/17/18, id המטרה, ms שנשארו בחלק]` (ערפל / נעלמת / יוצאת / מסתערת / מתאוששת). ב-15, מרגע שהמערבולת פתוחה, גם `x, z` שלה (13 שדות) — למצטרף באמצע. **שדה 4 (שקיפות) דולק רק בזמן הערפל** — זה הדגל שה-VANISH השתמש בו |
| `boss-ember` `e: "takeoff"` | שרת → חדר | 8 שנ' אחרי שהדרקון הגיע, אחר כך כל 16 (בשלב 2 כל 11.2) — כשיש מישהו חי עד 35 מ' | `{e, ms: 800, h: 6}` — ממריא 6 מ' (הלקוח מצייר את אותה עקומה כמו `liftOf`) |
| `boss-ember` `e: "spit"` | שרת → חדר | +800ms | `{e, ms: 917, lead: 583}` — `SpitUp`; הגחלים יוצאות מהפה `lead` ms פנימה |
| `boss-ember` `e: "rain"` | שרת → חדר | +583ms | `{e, r: 1.6, d: [[x, z, ms], ...]}` — כל נקודות הנחיתה (12, ועוד 2 לכל שחקן נוסף, עד 20) ובעוד כמה ms כל אחת (1200-1600) |
| `boss-ember` `e: "hit"` | שרת → חדר | 250ms אחרי שגחלת נחתה | `{e, p: [x, z], hit: [ids]}` — רק אם פגעה במישהו: 25 דרך `player-health` הרגיל. הבום והשלולית של כל גחלת הם `boss-boom` עם `k: "fire", e: 1` (רדיוס 1.5, 3 שנ') |
| `boss-ember` `e: "hover"` | שרת → חדר | בסוף ה-spit | `{e, ms}` — מרחף עד שהאחרונה נשפטה + 300ms |
| `boss-ember` `e: "land"` / `"done"` | שרת → חדר | +ms / +700ms | `{e, ms: 700}` / `{e}` |
| `boss` | שרת → חדר | כמו תמיד | `[..., 21/22/23/24, "", ms שנשארו בחלק]` (ממריא / יורק / מרחף / נוחת) — 11 שדות; הקודים של הדרקון הם 21-29 |
| `boss-tail` `e: "charge"` | שרת → חדר | כשמישהו חי עד 7 מ' ממנו: 3 שנ' אחרי שהגיע, אחר כך 7 שנ' אחרי שהקודם נגמר (בשלב 2 4.9), ו-1.5 שנ' לפחות מצלילה / גשם | `{e, ms: 790, y: yaw, dir: 1/-1, r: 8}` — מתכופף; `y` לאן הוא פונה, `dir` לאיזה צד יסתובב, `r` עד איפה הזנב |
| `boss-tail` `e: "sweep"` | שרת → חדר | +790ms | `{e, ms: 500}` — סיבוב אחד שלם (הלקוח מסובב; ה-yaw של השרת לא זז) |
| `boss-tail` `e: "hit"` | שרת → חדר | 250ms אחרי שהזנב עבר את השחקן | `{e, hit: [ids], p: [x, z], k: 13}` — 40 דרך `player-health` הרגיל; הדף של מי שנפגע זורק אותו מ-`p` החוצה ב-`k` מ'/ש' |
| `boss-tail` `e: "recover"` / `"done"` | שרת → חדר | +500ms / +625ms | `{e, ms: 625}` / `{e}` |
| `boss` | שרת → חדר | כמו תמיד | `[..., 25/26/27, "", ms שנשארו בחלק, dir]` (מתכופף / מסתובב / קם) — 12 שדות |
| `boss-roar-attack` `e: "swell"` | שרת → חדר | רק בשלב 2: 3 שנ' אחרי המעבר, אחר כך 15 שנ' אחרי הקודמת, כשיש מישהו חי עד 45 מ' (1.5 שנ' לפחות מצלילה / גשם / זנב) | `{e, ms: 1250, r: 45}` — החזה מתנפח (`Roar`). **לא** `boss-roar` — זה צליל הצלילה / המעבר לשלב 2 הישן |
| `boss-roar-attack` `e: "roar"` | שרת → חדר | +1250ms | `{e, ms: 958, hit: [ids], safe: [ids], daze: 2000}` — `hit` = עד 45 מ' עם קו פתוח על הקרקע (`nav.losClear`) → טשטוש ורעד אצלו; `safe` = עד 45 מ' מאחורי משהו. בלי נזק |
| `boss-roar-attack` `e: "done"` | שרת → חדר | +958ms | `{e}` |
| `boss` | שרת → חדר | כמו תמיד | `[..., 28/29, "", ms שנשארו בחלק]` (מתנפח / שואג) — 11 שדות |

---

## 10. רעיונות להמשך

1. **המכרה והמכלאה חסומים לבנדיטים.** זה היה כך תמיד, גם בדפדפן — הריפוד
   אוטם אותם ברשת. שלב 18 הפך את זה מבעיית ביצועים להתנהגות סבירה (בנדיט מגיע
   לגדר), אבל שחקן במכרה עדיין מוגן מבנדיטים שבחוץ. אפשר לפתוח: ריפוד קטן
   יותר באזורים האלה בלבד. **משנה משחק** — מקום מסתור שנעלם.
2. **המסות בקצוות** — אין להן תיבת התנגשות, אז בנדיטים יכולים ללכת לתוך
   הפינות שלהן וכדורים עוברים דרכן בבדיקת השרת. ייצוא תיבות גם להן.
3. **`CLAUDE.md` בריפו** במקום לצרף את המסמך הזה ידנית בכל שיחה.

---

## 11. איך לפתוח את השיחה הבאה

> ממשיכים את פרויקט Wild West Multiplayer. מצורף HANDOFF.md עם כל ההקשר.
> הקבצים נמצאים ב-`C:\Users\tamip\Desktop\CLOUD ME\משחק\`.
> תקרא את המסמך ואת הקבצים. שלבים 20-24 [הועלו / לא הועלו]. ממשיכים ב-[...].

שיטת העבודה שעבדה: **שלב אחד בכל פעם, נבדק מקומית לפני שמעלים, ובסוף
נאמר במפורש אילו קבצים להעלות.** אני מעלה ידנית ל-GitHub ומחכה ל-deploy.
